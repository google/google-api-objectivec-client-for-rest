// NOTE: This file was generated by the ServiceGenerator.

// ----------------------------------------------------------------------------
// API:
//   Service Management API (servicemanagement/v1)
// Description:
//   Google Service Management allows service producers to publish their
//   services on Google Cloud Platform so that they can be discovered and used
//   by service consumers.
// Documentation:
//   https://cloud.google.com/service-management/

#if SWIFT_PACKAGE || GTLR_USE_MODULAR_IMPORT
  @import GoogleAPIClientForRESTCore;
#elif GTLR_BUILT_AS_FRAMEWORK
  #import "GTLR/GTLRObject.h"
#else
  #import "GTLRObject.h"
#endif

#if GTLR_RUNTIME_VERSION != 3000
#error This file was generated by a different version of ServiceGenerator which is incompatible with this GTLR library source.
#endif

@class GTLRServiceManagement_Advice;
@class GTLRServiceManagement_Api;
@class GTLRServiceManagement_AuditConfig;
@class GTLRServiceManagement_AuditLogConfig;
@class GTLRServiceManagement_Authentication;
@class GTLRServiceManagement_AuthenticationRule;
@class GTLRServiceManagement_AuthProvider;
@class GTLRServiceManagement_AuthRequirement;
@class GTLRServiceManagement_Backend;
@class GTLRServiceManagement_BackendRule;
@class GTLRServiceManagement_Billing;
@class GTLRServiceManagement_BillingDestination;
@class GTLRServiceManagement_Binding;
@class GTLRServiceManagement_ChangeReport;
@class GTLRServiceManagement_ConfigChange;
@class GTLRServiceManagement_ConfigFile;
@class GTLRServiceManagement_ConfigSource;
@class GTLRServiceManagement_Context;
@class GTLRServiceManagement_ContextRule;
@class GTLRServiceManagement_Control;
@class GTLRServiceManagement_CustomError;
@class GTLRServiceManagement_CustomErrorRule;
@class GTLRServiceManagement_CustomHttpPattern;
@class GTLRServiceManagement_DeleteServiceStrategy;
@class GTLRServiceManagement_Diagnostic;
@class GTLRServiceManagement_Documentation;
@class GTLRServiceManagement_DocumentationRule;
@class GTLRServiceManagement_Endpoint;
@class GTLRServiceManagement_Enum;
@class GTLRServiceManagement_EnumValue;
@class GTLRServiceManagement_Expr;
@class GTLRServiceManagement_Field;
@class GTLRServiceManagement_GenerateConfigReportRequest_NewConfig;
@class GTLRServiceManagement_GenerateConfigReportRequest_OldConfig;
@class GTLRServiceManagement_GetPolicyOptions;
@class GTLRServiceManagement_Http;
@class GTLRServiceManagement_HttpRule;
@class GTLRServiceManagement_JwtLocation;
@class GTLRServiceManagement_LabelDescriptor;
@class GTLRServiceManagement_LogDescriptor;
@class GTLRServiceManagement_Logging;
@class GTLRServiceManagement_LoggingDestination;
@class GTLRServiceManagement_ManagedService;
@class GTLRServiceManagement_Method;
@class GTLRServiceManagement_MetricDescriptor;
@class GTLRServiceManagement_MetricDescriptorMetadata;
@class GTLRServiceManagement_MetricRule;
@class GTLRServiceManagement_MetricRule_MetricCosts;
@class GTLRServiceManagement_Mixin;
@class GTLRServiceManagement_MonitoredResourceDescriptor;
@class GTLRServiceManagement_Monitoring;
@class GTLRServiceManagement_MonitoringDestination;
@class GTLRServiceManagement_OAuthRequirements;
@class GTLRServiceManagement_Operation;
@class GTLRServiceManagement_Operation_Metadata;
@class GTLRServiceManagement_Operation_Response;
@class GTLRServiceManagement_Option;
@class GTLRServiceManagement_Option_Value;
@class GTLRServiceManagement_Page;
@class GTLRServiceManagement_Policy;
@class GTLRServiceManagement_Quota;
@class GTLRServiceManagement_QuotaLimit;
@class GTLRServiceManagement_QuotaLimit_Values;
@class GTLRServiceManagement_Rollout;
@class GTLRServiceManagement_Service;
@class GTLRServiceManagement_ServiceIdentity;
@class GTLRServiceManagement_SourceContext;
@class GTLRServiceManagement_SourceInfo;
@class GTLRServiceManagement_SourceInfo_SourceFiles_Item;
@class GTLRServiceManagement_Status;
@class GTLRServiceManagement_Status_Details_Item;
@class GTLRServiceManagement_Step;
@class GTLRServiceManagement_SystemParameter;
@class GTLRServiceManagement_SystemParameterRule;
@class GTLRServiceManagement_SystemParameters;
@class GTLRServiceManagement_TrafficPercentStrategy;
@class GTLRServiceManagement_TrafficPercentStrategy_Percentages;
@class GTLRServiceManagement_Type;
@class GTLRServiceManagement_Usage;
@class GTLRServiceManagement_UsageRule;

// Generated comments include content from the discovery document; avoid them
// causing warnings since clang's checks are some what arbitrary.
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdocumentation"

NS_ASSUME_NONNULL_BEGIN

// ----------------------------------------------------------------------------
// Constants - For some of the classes' properties below.

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Api.syntax

/**
 *  Syntax `proto2`.
 *
 *  Value: "SYNTAX_PROTO2"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Api_Syntax_SyntaxProto2;
/**
 *  Syntax `proto3`.
 *
 *  Value: "SYNTAX_PROTO3"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Api_Syntax_SyntaxProto3;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_AuditLogConfig.logType

/**
 *  Admin reads. Example: CloudIAM getIamPolicy
 *
 *  Value: "ADMIN_READ"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_AuditLogConfig_LogType_AdminRead;
/**
 *  Data reads. Example: CloudSQL Users list
 *
 *  Value: "DATA_READ"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_AuditLogConfig_LogType_DataRead;
/**
 *  Data writes. Example: CloudSQL Users create
 *
 *  Value: "DATA_WRITE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_AuditLogConfig_LogType_DataWrite;
/**
 *  Default case. Should never be this.
 *
 *  Value: "LOG_TYPE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_AuditLogConfig_LogType_LogTypeUnspecified;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_BackendRule.pathTranslation

/**
 *  The request path will be appended to the backend address.
 *  # Examples
 *  Given the following operation config:
 *  Method path: /api/company/{cid}/user/{uid}
 *  Backend address: https://example.appspot.com
 *  Requests to the following request paths will call the backend at the
 *  translated path:
 *  Request path: /api/company/widgetworks/user/johndoe
 *  Translated:
 *  https://example.appspot.com/api/company/widgetworks/user/johndoe
 *  Request path: /api/company/widgetworks/user/johndoe?timezone=EST
 *  Translated:
 *  https://example.appspot.com/api/company/widgetworks/user/johndoe?timezone=EST
 *
 *  Value: "APPEND_PATH_TO_ADDRESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_BackendRule_PathTranslation_AppendPathToAddress;
/**
 *  Use the backend address as-is, with no modification to the path. If the
 *  URL pattern contains variables, the variable names and values will be
 *  appended to the query string. If a query string parameter and a URL
 *  pattern variable have the same name, this may result in duplicate keys in
 *  the query string.
 *  # Examples
 *  Given the following operation config:
 *  Method path: /api/company/{cid}/user/{uid}
 *  Backend address: https://example.cloudfunctions.net/getUser
 *  Requests to the following request paths will call the backend at the
 *  translated path:
 *  Request path: /api/company/widgetworks/user/johndoe
 *  Translated:
 *  https://example.cloudfunctions.net/getUser?cid=widgetworks&uid=johndoe
 *  Request path: /api/company/widgetworks/user/johndoe?timezone=EST
 *  Translated:
 *  https://example.cloudfunctions.net/getUser?timezone=EST&cid=widgetworks&uid=johndoe
 *
 *  Value: "CONSTANT_ADDRESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_BackendRule_PathTranslation_ConstantAddress;
/** Value: "PATH_TRANSLATION_UNSPECIFIED" */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_BackendRule_PathTranslation_PathTranslationUnspecified;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_ConfigChange.changeType

/**
 *  The changed object exists in the 'new' service configuration, but not
 *  in the 'old' service configuration.
 *
 *  Value: "ADDED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigChange_ChangeType_Added;
/**
 *  No value was provided.
 *
 *  Value: "CHANGE_TYPE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigChange_ChangeType_ChangeTypeUnspecified;
/**
 *  The changed object exists in both service configurations, but its value
 *  is different.
 *
 *  Value: "MODIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigChange_ChangeType_Modified;
/**
 *  The changed object exists in the 'old' service configuration, but not
 *  in the 'new' service configuration.
 *
 *  Value: "REMOVED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigChange_ChangeType_Removed;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_ConfigFile.fileType

/**
 *  FileDescriptorSet, generated by protoc.
 *  To generate, use protoc with imports and source info included.
 *  For an example test.proto file, the following command would put the value
 *  in a new file named out.pb.
 *  $protoc --include_imports --include_source_info test.proto -o out.pb
 *
 *  Value: "FILE_DESCRIPTOR_SET_PROTO"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigFile_FileType_FileDescriptorSetProto;
/**
 *  Unknown file type.
 *
 *  Value: "FILE_TYPE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigFile_FileType_FileTypeUnspecified;
/**
 *  OpenAPI specification, serialized in JSON.
 *
 *  Value: "OPEN_API_JSON"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigFile_FileType_OpenApiJson;
/**
 *  OpenAPI specification, serialized in YAML.
 *
 *  Value: "OPEN_API_YAML"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigFile_FileType_OpenApiYaml;
/**
 *  Uncompiled Proto file. Used for storage and display purposes only,
 *  currently server-side compilation is not supported. Should match the
 *  inputs to 'protoc' command used to generated FILE_DESCRIPTOR_SET_PROTO. A
 *  file of this type can only be included if at least one file of type
 *  FILE_DESCRIPTOR_SET_PROTO is included.
 *
 *  Value: "PROTO_FILE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigFile_FileType_ProtoFile;
/**
 *  YAML-specification of service.
 *
 *  Value: "SERVICE_CONFIG_YAML"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_ConfigFile_FileType_ServiceConfigYaml;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Diagnostic.kind

/**
 *  Only errors
 *
 *  Value: "ERROR"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Diagnostic_Kind_Error;
/**
 *  Warnings and errors
 *
 *  Value: "WARNING"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Diagnostic_Kind_Warning;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Enum.syntax

/**
 *  Syntax `proto2`.
 *
 *  Value: "SYNTAX_PROTO2"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Enum_Syntax_SyntaxProto2;
/**
 *  Syntax `proto3`.
 *
 *  Value: "SYNTAX_PROTO3"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Enum_Syntax_SyntaxProto3;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Field.cardinality

/**
 *  For optional fields.
 *
 *  Value: "CARDINALITY_OPTIONAL"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Cardinality_CardinalityOptional;
/**
 *  For repeated fields.
 *
 *  Value: "CARDINALITY_REPEATED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Cardinality_CardinalityRepeated;
/**
 *  For required fields. Proto2 syntax only.
 *
 *  Value: "CARDINALITY_REQUIRED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Cardinality_CardinalityRequired;
/**
 *  For fields with unknown cardinality.
 *
 *  Value: "CARDINALITY_UNKNOWN"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Cardinality_CardinalityUnknown;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Field.kind

/**
 *  Field type bool.
 *
 *  Value: "TYPE_BOOL"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeBool;
/**
 *  Field type bytes.
 *
 *  Value: "TYPE_BYTES"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeBytes;
/**
 *  Field type double.
 *
 *  Value: "TYPE_DOUBLE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeDouble;
/**
 *  Field type enum.
 *
 *  Value: "TYPE_ENUM"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeEnum;
/**
 *  Field type fixed32.
 *
 *  Value: "TYPE_FIXED32"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeFixed32;
/**
 *  Field type fixed64.
 *
 *  Value: "TYPE_FIXED64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeFixed64;
/**
 *  Field type float.
 *
 *  Value: "TYPE_FLOAT"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeFloat;
/**
 *  Field type group. Proto2 syntax only, and deprecated.
 *
 *  Value: "TYPE_GROUP"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeGroup;
/**
 *  Field type int32.
 *
 *  Value: "TYPE_INT32"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeInt32;
/**
 *  Field type int64.
 *
 *  Value: "TYPE_INT64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeInt64;
/**
 *  Field type message.
 *
 *  Value: "TYPE_MESSAGE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeMessage;
/**
 *  Field type sfixed32.
 *
 *  Value: "TYPE_SFIXED32"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeSfixed32;
/**
 *  Field type sfixed64.
 *
 *  Value: "TYPE_SFIXED64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeSfixed64;
/**
 *  Field type sint32.
 *
 *  Value: "TYPE_SINT32"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeSint32;
/**
 *  Field type sint64.
 *
 *  Value: "TYPE_SINT64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeSint64;
/**
 *  Field type string.
 *
 *  Value: "TYPE_STRING"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeString;
/**
 *  Field type uint32.
 *
 *  Value: "TYPE_UINT32"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeUint32;
/**
 *  Field type uint64.
 *
 *  Value: "TYPE_UINT64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeUint64;
/**
 *  Field type unknown.
 *
 *  Value: "TYPE_UNKNOWN"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Field_Kind_TypeUnknown;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_LabelDescriptor.valueType

/**
 *  Boolean; true or false.
 *
 *  Value: "BOOL"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_LabelDescriptor_ValueType_Bool;
/**
 *  A 64-bit signed integer.
 *
 *  Value: "INT64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_LabelDescriptor_ValueType_Int64;
/**
 *  A variable-length string. This is the default.
 *
 *  Value: "STRING"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_LabelDescriptor_ValueType_String;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Method.syntax

/**
 *  Syntax `proto2`.
 *
 *  Value: "SYNTAX_PROTO2"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Method_Syntax_SyntaxProto2;
/**
 *  Syntax `proto3`.
 *
 *  Value: "SYNTAX_PROTO3"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Method_Syntax_SyntaxProto3;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_MetricDescriptor.launchStage

/**
 *  Alpha is a limited availability test for releases before they are cleared
 *  for widespread use. By Alpha, all significant design issues are resolved
 *  and we are in the process of verifying functionality. Alpha customers
 *  need to apply for access, agree to applicable terms, and have their
 *  projects whitelisted. Alpha releases don’t have to be feature complete,
 *  no SLAs are provided, and there are no technical support obligations, but
 *  they will be far enough along that customers can actually use them in
 *  test environments or for limited-use tests -- just like they would in
 *  normal production cases.
 *
 *  Value: "ALPHA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_Alpha;
/**
 *  Beta is the point at which we are ready to open a release for any
 *  customer to use. There are no SLA or technical support obligations in a
 *  Beta release. Products will be complete from a feature perspective, but
 *  may have some open outstanding issues. Beta releases are suitable for
 *  limited production use cases.
 *
 *  Value: "BETA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_Beta;
/**
 *  Deprecated features are scheduled to be shut down and removed. For more
 *  information, see the “Deprecation Policy” section of our [Terms of
 *  Service](https://cloud.google.com/terms/)
 *  and the [Google Cloud Platform Subject to the Deprecation
 *  Policy](https://cloud.google.com/terms/deprecation) documentation.
 *
 *  Value: "DEPRECATED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_Deprecated;
/**
 *  Early Access features are limited to a closed group of testers. To use
 *  these features, you must sign up in advance and sign a Trusted Tester
 *  agreement (which includes confidentiality provisions). These features may
 *  be unstable, changed in backward-incompatible ways, and are not
 *  guaranteed to be released.
 *
 *  Value: "EARLY_ACCESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_EarlyAccess;
/**
 *  GA features are open to all developers and are considered stable and
 *  fully qualified for production use.
 *
 *  Value: "GA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_Ga;
/**
 *  Do not use this default value.
 *
 *  Value: "LAUNCH_STAGE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_LaunchStageUnspecified;
/**
 *  Prelaunch features are hidden from users and are only visible internally.
 *
 *  Value: "PRELAUNCH"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_Prelaunch;
/**
 *  The feature is not yet implemented. Users can not use it.
 *
 *  Value: "UNIMPLEMENTED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_LaunchStage_Unimplemented;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_MetricDescriptor.metricKind

/**
 *  A value accumulated over a time interval. Cumulative
 *  measurements in a time series should have the same start time
 *  and increasing end times, until an event resets the cumulative
 *  value to zero and sets a new start time for the following
 *  points.
 *
 *  Value: "CUMULATIVE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_MetricKind_Cumulative;
/**
 *  The change in a value during a time interval.
 *
 *  Value: "DELTA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_MetricKind_Delta;
/**
 *  An instantaneous measurement of a value.
 *
 *  Value: "GAUGE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_MetricKind_Gauge;
/**
 *  Do not use this default value.
 *
 *  Value: "METRIC_KIND_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_MetricKind_MetricKindUnspecified;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_MetricDescriptor.valueType

/**
 *  The value is a boolean.
 *  This value type can be used only if the metric kind is `GAUGE`.
 *
 *  Value: "BOOL"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_Bool;
/**
 *  The value is a `Distribution`.
 *
 *  Value: "DISTRIBUTION"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_Distribution;
/**
 *  The value is a double precision floating point number.
 *
 *  Value: "DOUBLE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_Double;
/**
 *  The value is a signed 64-bit integer.
 *
 *  Value: "INT64"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_Int64;
/**
 *  The value is money.
 *
 *  Value: "MONEY"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_Money;
/**
 *  The value is a text string.
 *  This value type can be used only if the metric kind is `GAUGE`.
 *
 *  Value: "STRING"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_String;
/**
 *  Do not use this default value.
 *
 *  Value: "VALUE_TYPE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptor_ValueType_ValueTypeUnspecified;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_MetricDescriptorMetadata.launchStage

/**
 *  Alpha is a limited availability test for releases before they are cleared
 *  for widespread use. By Alpha, all significant design issues are resolved
 *  and we are in the process of verifying functionality. Alpha customers
 *  need to apply for access, agree to applicable terms, and have their
 *  projects whitelisted. Alpha releases don’t have to be feature complete,
 *  no SLAs are provided, and there are no technical support obligations, but
 *  they will be far enough along that customers can actually use them in
 *  test environments or for limited-use tests -- just like they would in
 *  normal production cases.
 *
 *  Value: "ALPHA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Alpha;
/**
 *  Beta is the point at which we are ready to open a release for any
 *  customer to use. There are no SLA or technical support obligations in a
 *  Beta release. Products will be complete from a feature perspective, but
 *  may have some open outstanding issues. Beta releases are suitable for
 *  limited production use cases.
 *
 *  Value: "BETA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Beta;
/**
 *  Deprecated features are scheduled to be shut down and removed. For more
 *  information, see the “Deprecation Policy” section of our [Terms of
 *  Service](https://cloud.google.com/terms/)
 *  and the [Google Cloud Platform Subject to the Deprecation
 *  Policy](https://cloud.google.com/terms/deprecation) documentation.
 *
 *  Value: "DEPRECATED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Deprecated;
/**
 *  Early Access features are limited to a closed group of testers. To use
 *  these features, you must sign up in advance and sign a Trusted Tester
 *  agreement (which includes confidentiality provisions). These features may
 *  be unstable, changed in backward-incompatible ways, and are not
 *  guaranteed to be released.
 *
 *  Value: "EARLY_ACCESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_EarlyAccess;
/**
 *  GA features are open to all developers and are considered stable and
 *  fully qualified for production use.
 *
 *  Value: "GA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Ga;
/**
 *  Do not use this default value.
 *
 *  Value: "LAUNCH_STAGE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_LaunchStageUnspecified;
/**
 *  Prelaunch features are hidden from users and are only visible internally.
 *
 *  Value: "PRELAUNCH"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Prelaunch;
/**
 *  The feature is not yet implemented. Users can not use it.
 *
 *  Value: "UNIMPLEMENTED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Unimplemented;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_MonitoredResourceDescriptor.launchStage

/**
 *  Alpha is a limited availability test for releases before they are cleared
 *  for widespread use. By Alpha, all significant design issues are resolved
 *  and we are in the process of verifying functionality. Alpha customers
 *  need to apply for access, agree to applicable terms, and have their
 *  projects whitelisted. Alpha releases don’t have to be feature complete,
 *  no SLAs are provided, and there are no technical support obligations, but
 *  they will be far enough along that customers can actually use them in
 *  test environments or for limited-use tests -- just like they would in
 *  normal production cases.
 *
 *  Value: "ALPHA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Alpha;
/**
 *  Beta is the point at which we are ready to open a release for any
 *  customer to use. There are no SLA or technical support obligations in a
 *  Beta release. Products will be complete from a feature perspective, but
 *  may have some open outstanding issues. Beta releases are suitable for
 *  limited production use cases.
 *
 *  Value: "BETA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Beta;
/**
 *  Deprecated features are scheduled to be shut down and removed. For more
 *  information, see the “Deprecation Policy” section of our [Terms of
 *  Service](https://cloud.google.com/terms/)
 *  and the [Google Cloud Platform Subject to the Deprecation
 *  Policy](https://cloud.google.com/terms/deprecation) documentation.
 *
 *  Value: "DEPRECATED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Deprecated;
/**
 *  Early Access features are limited to a closed group of testers. To use
 *  these features, you must sign up in advance and sign a Trusted Tester
 *  agreement (which includes confidentiality provisions). These features may
 *  be unstable, changed in backward-incompatible ways, and are not
 *  guaranteed to be released.
 *
 *  Value: "EARLY_ACCESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_EarlyAccess;
/**
 *  GA features are open to all developers and are considered stable and
 *  fully qualified for production use.
 *
 *  Value: "GA"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Ga;
/**
 *  Do not use this default value.
 *
 *  Value: "LAUNCH_STAGE_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_LaunchStageUnspecified;
/**
 *  Prelaunch features are hidden from users and are only visible internally.
 *
 *  Value: "PRELAUNCH"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Prelaunch;
/**
 *  The feature is not yet implemented. Users can not use it.
 *
 *  Value: "UNIMPLEMENTED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Unimplemented;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Rollout.status

/**
 *  The Rollout has been cancelled. This can happen if you have overlapping
 *  Rollout pushes, and the previous ones will be cancelled.
 *
 *  Value: "CANCELLED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_Cancelled;
/**
 *  The Rollout has failed and the rollback attempt has failed too.
 *
 *  Value: "FAILED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_Failed;
/**
 *  The Rollout has failed and rolled back to the previous successful
 *  Rollout.
 *
 *  Value: "FAILED_ROLLED_BACK"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_FailedRolledBack;
/**
 *  The Rollout is in progress.
 *
 *  Value: "IN_PROGRESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_InProgress;
/**
 *  The Rollout has not started yet and is pending for execution.
 *
 *  Value: "PENDING"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_Pending;
/**
 *  No status specified.
 *
 *  Value: "ROLLOUT_STATUS_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_RolloutStatusUnspecified;
/**
 *  The Rollout has completed successfully.
 *
 *  Value: "SUCCESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Rollout_Status_Success;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Step.status

/**
 *  The operation or step has completed with cancellation.
 *
 *  Value: "CANCELLED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Step_Status_Cancelled;
/**
 *  The operation or step has completed without errors.
 *
 *  Value: "DONE"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Step_Status_Done;
/**
 *  The operation or step has completed with errors. If the operation is
 *  rollbackable, the rollback completed with errors too.
 *
 *  Value: "FAILED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Step_Status_Failed;
/**
 *  The operation or step is in progress.
 *
 *  Value: "IN_PROGRESS"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Step_Status_InProgress;
/**
 *  The operation or step has not started yet.
 *
 *  Value: "NOT_STARTED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Step_Status_NotStarted;
/**
 *  Unspecifed code.
 *
 *  Value: "STATUS_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Step_Status_StatusUnspecified;

// ----------------------------------------------------------------------------
// GTLRServiceManagement_Type.syntax

/**
 *  Syntax `proto2`.
 *
 *  Value: "SYNTAX_PROTO2"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Type_Syntax_SyntaxProto2;
/**
 *  Syntax `proto3`.
 *
 *  Value: "SYNTAX_PROTO3"
 */
FOUNDATION_EXTERN NSString * const kGTLRServiceManagement_Type_Syntax_SyntaxProto3;

/**
 *  Generated advice about this change, used for providing more
 *  information about how a change will affect the existing service.
 */
@interface GTLRServiceManagement_Advice : GTLRObject

/**
 *  Useful description for why this advice was applied and what actions should
 *  be taken to mitigate any implied risks.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

@end


/**
 *  Api is a light-weight descriptor for an API Interface.
 *  Interfaces are also described as "protocol buffer services" in some
 *  contexts,
 *  such as by the "service" keyword in a .proto file, but they are different
 *  from API Services, which represent a concrete implementation of an interface
 *  as opposed to simply a description of methods and bindings. They are also
 *  sometimes simply referred to as "APIs" in other contexts, such as the name
 *  of
 *  this message itself. See https://cloud.google.com/apis/design/glossary for
 *  detailed terminology.
 */
@interface GTLRServiceManagement_Api : GTLRObject

/** The methods of this interface, in unspecified order. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Method *> *methods;

/** Included interfaces. See Mixin. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Mixin *> *mixins;

/**
 *  The fully qualified name of this interface, including package name
 *  followed by the interface's simple name.
 */
@property(nonatomic, copy, nullable) NSString *name;

/** Any metadata attached to the interface. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Option *> *options;

/**
 *  Source context for the protocol buffer service represented by this
 *  message.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_SourceContext *sourceContext;

/**
 *  The source syntax of the service.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Api_Syntax_SyntaxProto2 Syntax `proto2`.
 *        (Value: "SYNTAX_PROTO2")
 *    @arg @c kGTLRServiceManagement_Api_Syntax_SyntaxProto3 Syntax `proto3`.
 *        (Value: "SYNTAX_PROTO3")
 */
@property(nonatomic, copy, nullable) NSString *syntax;

/**
 *  A version string for this interface. If specified, must have the form
 *  `major-version.minor-version`, as in `1.10`. If the minor version is
 *  omitted, it defaults to zero. If the entire version field is empty, the
 *  major version is derived from the package name, as outlined below. If the
 *  field is not empty, the version in the package name will be verified to be
 *  consistent with what is provided here.
 *  The versioning schema uses [semantic
 *  versioning](http://semver.org) where the major version number
 *  indicates a breaking change and the minor version an additive,
 *  non-breaking change. Both version numbers are signals to users
 *  what to expect from different versions, and should be carefully
 *  chosen based on the product plan.
 *  The major version is also reflected in the package name of the
 *  interface, which must end in `v<major-version>`, as in
 *  `google.feature.v1`. For major versions 0 and 1, the suffix can
 *  be omitted. Zero major versions must only be used for
 *  experimental, non-GA interfaces.
 */
@property(nonatomic, copy, nullable) NSString *version;

@end


/**
 *  Specifies the audit configuration for a service.
 *  The configuration determines which permission types are logged, and what
 *  identities, if any, are exempted from logging.
 *  An AuditConfig must have one or more AuditLogConfigs.
 *  If there are AuditConfigs for both `allServices` and a specific service,
 *  the union of the two AuditConfigs is used for that service: the log_types
 *  specified in each AuditConfig are enabled, and the exempted_members in each
 *  AuditLogConfig are exempted.
 *  Example Policy with multiple AuditConfigs:
 *  {
 *  "audit_configs": [
 *  {
 *  "service": "allServices"
 *  "audit_log_configs": [
 *  {
 *  "log_type": "DATA_READ",
 *  "exempted_members": [
 *  "user:jose\@example.com"
 *  ]
 *  },
 *  {
 *  "log_type": "DATA_WRITE",
 *  },
 *  {
 *  "log_type": "ADMIN_READ",
 *  }
 *  ]
 *  },
 *  {
 *  "service": "sampleservice.googleapis.com"
 *  "audit_log_configs": [
 *  {
 *  "log_type": "DATA_READ",
 *  },
 *  {
 *  "log_type": "DATA_WRITE",
 *  "exempted_members": [
 *  "user:aliya\@example.com"
 *  ]
 *  }
 *  ]
 *  }
 *  ]
 *  }
 *  For sampleservice, this policy enables DATA_READ, DATA_WRITE and ADMIN_READ
 *  logging. It also exempts jose\@example.com from DATA_READ logging, and
 *  aliya\@example.com from DATA_WRITE logging.
 */
@interface GTLRServiceManagement_AuditConfig : GTLRObject

/** The configuration for logging of each type of permission. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_AuditLogConfig *> *auditLogConfigs;

/**
 *  Specifies a service that will be enabled for audit logging.
 *  For example, `storage.googleapis.com`, `cloudsql.googleapis.com`.
 *  `allServices` is a special value that covers all services.
 */
@property(nonatomic, copy, nullable) NSString *service;

@end


/**
 *  Provides the configuration for logging a type of permissions.
 *  Example:
 *  {
 *  "audit_log_configs": [
 *  {
 *  "log_type": "DATA_READ",
 *  "exempted_members": [
 *  "user:jose\@example.com"
 *  ]
 *  },
 *  {
 *  "log_type": "DATA_WRITE",
 *  }
 *  ]
 *  }
 *  This enables 'DATA_READ' and 'DATA_WRITE' logging, while exempting
 *  jose\@example.com from DATA_READ logging.
 */
@interface GTLRServiceManagement_AuditLogConfig : GTLRObject

/**
 *  Specifies the identities that do not cause logging for this type of
 *  permission.
 *  Follows the same format of Binding.members.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *exemptedMembers;

/**
 *  The log type that this config enables.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_AuditLogConfig_LogType_AdminRead Admin
 *        reads. Example: CloudIAM getIamPolicy (Value: "ADMIN_READ")
 *    @arg @c kGTLRServiceManagement_AuditLogConfig_LogType_DataRead Data reads.
 *        Example: CloudSQL Users list (Value: "DATA_READ")
 *    @arg @c kGTLRServiceManagement_AuditLogConfig_LogType_DataWrite Data
 *        writes. Example: CloudSQL Users create (Value: "DATA_WRITE")
 *    @arg @c kGTLRServiceManagement_AuditLogConfig_LogType_LogTypeUnspecified
 *        Default case. Should never be this. (Value: "LOG_TYPE_UNSPECIFIED")
 */
@property(nonatomic, copy, nullable) NSString *logType;

@end


/**
 *  `Authentication` defines the authentication configuration for an API.
 *  Example for an API targeted for external use:
 *  name: calendar.googleapis.com
 *  authentication:
 *  providers:
 *  - id: google_calendar_auth
 *  jwks_uri: https://www.googleapis.com/oauth2/v1/certs
 *  issuer: https://securetoken.google.com
 *  rules:
 *  - selector: "*"
 *  requirements:
 *  provider_id: google_calendar_auth
 */
@interface GTLRServiceManagement_Authentication : GTLRObject

/** Defines a set of authentication providers that a service supports. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_AuthProvider *> *providers;

/**
 *  A list of authentication rules that apply to individual API methods.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_AuthenticationRule *> *rules;

@end


/**
 *  Authentication rules for the service.
 *  By default, if a method has any authentication requirements, every request
 *  must include a valid credential matching one of the requirements.
 *  It's an error to include more than one kind of credential in a single
 *  request.
 *  If a method doesn't have any auth requirements, request credentials will be
 *  ignored.
 */
@interface GTLRServiceManagement_AuthenticationRule : GTLRObject

/**
 *  If true, the service accepts API keys without any other credential.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *allowWithoutCredential;

/** The requirements for OAuth credentials. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_OAuthRequirements *oauth;

/** Requirements for additional authentication providers. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_AuthRequirement *> *requirements;

/**
 *  Selects the methods to which this rule applies.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  Configuration for an authentication provider, including support for
 *  [JSON Web Token
 *  (JWT)](https://tools.ietf.org/html/draft-ietf-oauth-json-web-token-32).
 */
@interface GTLRServiceManagement_AuthProvider : GTLRObject

/**
 *  The list of JWT
 *  [audiences](https://tools.ietf.org/html/draft-ietf-oauth-json-web-token-32#section-4.1.3).
 *  that are allowed to access. A JWT containing any of these audiences will
 *  be accepted. When this setting is absent, JWTs with audiences:
 *  - "https://[service.name]/[google.protobuf.Api.name]"
 *  - "https://[service.name]/"
 *  will be accepted.
 *  For example, if no audiences are in the setting, LibraryService API will
 *  accept JWTs with the following audiences:
 *  -
 *  https://library-example.googleapis.com/google.example.library.v1.LibraryService
 *  - https://library-example.googleapis.com/
 *  Example:
 *  audiences: bookstore_android.apps.googleusercontent.com,
 *  bookstore_web.apps.googleusercontent.com
 */
@property(nonatomic, copy, nullable) NSString *audiences;

/**
 *  Redirect URL if JWT token is required but not present or is expired.
 *  Implement authorizationUrl of securityDefinitions in OpenAPI spec.
 */
@property(nonatomic, copy, nullable) NSString *authorizationUrl;

/**
 *  The unique identifier of the auth provider. It will be referred to by
 *  `AuthRequirement.provider_id`.
 *  Example: "bookstore_auth".
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 */
@property(nonatomic, copy, nullable) NSString *identifier;

/**
 *  Identifies the principal that issued the JWT. See
 *  https://tools.ietf.org/html/draft-ietf-oauth-json-web-token-32#section-4.1.1
 *  Usually a URL or an email address.
 *  Example: https://securetoken.google.com
 *  Example: 1234567-compute\@developer.gserviceaccount.com
 */
@property(nonatomic, copy, nullable) NSString *issuer;

/**
 *  URL of the provider's public key set to validate signature of the JWT. See
 *  [OpenID
 *  Discovery](https://openid.net/specs/openid-connect-discovery-1_0.html#ProviderMetadata).
 *  Optional if the key set document:
 *  - can be retrieved from
 *  [OpenID
 *  Discovery](https://openid.net/specs/openid-connect-discovery-1_0.html of
 *  the issuer.
 *  - can be inferred from the email domain of the issuer (e.g. a Google
 *  service account).
 *  Example: https://www.googleapis.com/oauth2/v1/certs
 */
@property(nonatomic, copy, nullable) NSString *jwksUri;

/**
 *  Defines the locations to extract the JWT.
 *  JWT locations can be either from HTTP headers or URL query parameters.
 *  The rule is that the first match wins. The checking order is: checking
 *  all headers first, then URL query parameters.
 *  If not specified, default to use following 3 locations:
 *  1) Authorization: Bearer
 *  2) x-goog-iap-jwt-assertion
 *  3) access_token query parameter
 *  Default locations can be specified as followings:
 *  jwt_locations:
 *  - header: Authorization
 *  value_prefix: "Bearer "
 *  - header: x-goog-iap-jwt-assertion
 *  - query: access_token
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_JwtLocation *> *jwtLocations;

@end


/**
 *  User-defined authentication requirements, including support for
 *  [JSON Web Token
 *  (JWT)](https://tools.ietf.org/html/draft-ietf-oauth-json-web-token-32).
 */
@interface GTLRServiceManagement_AuthRequirement : GTLRObject

/**
 *  NOTE: This will be deprecated soon, once AuthProvider.audiences is
 *  implemented and accepted in all the runtime components.
 *  The list of JWT
 *  [audiences](https://tools.ietf.org/html/draft-ietf-oauth-json-web-token-32#section-4.1.3).
 *  that are allowed to access. A JWT containing any of these audiences will
 *  be accepted. When this setting is absent, only JWTs with audience
 *  "https://Service_name/API_name"
 *  will be accepted. For example, if no audiences are in the setting,
 *  LibraryService API will only accept JWTs with the following audience
 *  "https://library-example.googleapis.com/google.example.library.v1.LibraryService".
 *  Example:
 *  audiences: bookstore_android.apps.googleusercontent.com,
 *  bookstore_web.apps.googleusercontent.com
 */
@property(nonatomic, copy, nullable) NSString *audiences;

/**
 *  id from authentication provider.
 *  Example:
 *  provider_id: bookstore_auth
 */
@property(nonatomic, copy, nullable) NSString *providerId;

@end


/**
 *  `Backend` defines the backend configuration for a service.
 */
@interface GTLRServiceManagement_Backend : GTLRObject

/**
 *  A list of API backend rules that apply to individual API methods.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_BackendRule *> *rules;

@end


/**
 *  A backend rule provides configuration for an individual API element.
 */
@interface GTLRServiceManagement_BackendRule : GTLRObject

/**
 *  The address of the API backend.
 *  The scheme is used to determine the backend protocol and security.
 *  The following schemes are accepted:
 *  SCHEME PROTOCOL SECURITY
 *  http:// HTTP None
 *  https:// HTTP TLS
 *  grpc:// gRPC None
 *  grpcs:// gRPC TLS
 *  It is recommended to explicitly include a scheme. Leaving out the scheme
 *  may cause constrasting behaviors across platforms.
 *  If the port is unspecified, the default is:
 *  - 80 for schemes without TLS
 *  - 443 for schemes with TLS
 *  For HTTP backends, use protocol
 *  to specify the protocol version.
 */
@property(nonatomic, copy, nullable) NSString *address;

/**
 *  The number of seconds to wait for a response from a request. The default
 *  varies based on the request protocol and deployment environment.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(nonatomic, strong, nullable) NSNumber *deadline;

/**
 *  When disable_auth is true, a JWT ID token won't be generated and the
 *  original "Authorization" HTTP header will be preserved. If the header is
 *  used to carry the original token and is expected by the backend, this
 *  field must be set to true to preserve the header.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *disableAuth;

/**
 *  The JWT audience is used when generating a JWT ID token for the backend.
 *  This ID token will be added in the HTTP "authorization" header, and sent
 *  to the backend.
 */
@property(nonatomic, copy, nullable) NSString *jwtAudience;

/**
 *  Minimum deadline in seconds needed for this method. Calls having deadline
 *  value lower than this will be rejected.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(nonatomic, strong, nullable) NSNumber *minDeadline;

/**
 *  The number of seconds to wait for the completion of a long running
 *  operation. The default is no deadline.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(nonatomic, strong, nullable) NSNumber *operationDeadline;

/**
 *  pathTranslation
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_BackendRule_PathTranslation_AppendPathToAddress
 *        The request path will be appended to the backend address.
 *        # Examples
 *        Given the following operation config:
 *        Method path: /api/company/{cid}/user/{uid}
 *        Backend address: https://example.appspot.com
 *        Requests to the following request paths will call the backend at the
 *        translated path:
 *        Request path: /api/company/widgetworks/user/johndoe
 *        Translated:
 *        https://example.appspot.com/api/company/widgetworks/user/johndoe
 *        Request path: /api/company/widgetworks/user/johndoe?timezone=EST
 *        Translated:
 *        https://example.appspot.com/api/company/widgetworks/user/johndoe?timezone=EST
 *        (Value: "APPEND_PATH_TO_ADDRESS")
 *    @arg @c kGTLRServiceManagement_BackendRule_PathTranslation_ConstantAddress
 *        Use the backend address as-is, with no modification to the path. If
 *        the
 *        URL pattern contains variables, the variable names and values will be
 *        appended to the query string. If a query string parameter and a URL
 *        pattern variable have the same name, this may result in duplicate keys
 *        in
 *        the query string.
 *        # Examples
 *        Given the following operation config:
 *        Method path: /api/company/{cid}/user/{uid}
 *        Backend address: https://example.cloudfunctions.net/getUser
 *        Requests to the following request paths will call the backend at the
 *        translated path:
 *        Request path: /api/company/widgetworks/user/johndoe
 *        Translated:
 *        https://example.cloudfunctions.net/getUser?cid=widgetworks&uid=johndoe
 *        Request path: /api/company/widgetworks/user/johndoe?timezone=EST
 *        Translated:
 *        https://example.cloudfunctions.net/getUser?timezone=EST&cid=widgetworks&uid=johndoe
 *        (Value: "CONSTANT_ADDRESS")
 *    @arg @c kGTLRServiceManagement_BackendRule_PathTranslation_PathTranslationUnspecified
 *        Value "PATH_TRANSLATION_UNSPECIFIED"
 */
@property(nonatomic, copy, nullable) NSString *pathTranslation;

/**
 *  The protocol used for sending a request to the backend.
 *  The supported values are "http/1.1" and "h2".
 *  The default value is inferred from the scheme in the
 *  address field:
 *  SCHEME PROTOCOL
 *  http:// http/1.1
 *  https:// http/1.1
 *  grpc:// h2
 *  grpcs:// h2
 *  For secure HTTP backends (https://) that support HTTP/2, set this field
 *  to "h2" for improved performance.
 *  Configuring this field to non-default values is only supported for secure
 *  HTTP backends. This field will be ignored for all other backends.
 *  See
 *  https://www.iana.org/assignments/tls-extensiontype-values/tls-extensiontype-values.xhtml#alpn-protocol-ids
 *  for more details on the supported values.
 */
@property(nonatomic, copy, nullable) NSString *protocol;

/**
 *  Unimplemented. Do not use.
 *  The new name the selected proto elements should be renamed to.
 *  The package, the service and the method can all be renamed.
 *  The backend server should implement the renamed proto. However, clients
 *  should call the original method, and ESF routes the traffic to the renamed
 *  method.
 *  HTTP clients should call the URL mapped to the original method.
 *  gRPC and Stubby clients should call the original method with package name.
 *  For legacy reasons, ESF allows Stubby clients to call with the
 *  short name (without the package name). However, for API Versioning(or
 *  multiple methods mapped to the same short name), all Stubby clients must
 *  call the method's full name with the package name, otherwise the first one
 *  (selector) wins.
 *  If this `rename_to` is specified with a trailing `*`, the `selector` must
 *  be specified with a trailing `*` as well. The all element short names
 *  matched by the `*` in the selector will be kept in the `rename_to`.
 *  For example,
 *  rename_rules:
 *  - selector: |-
 *  google.example.library.v1.*
 *  rename_to: google.example.library.*
 *  The selector matches `google.example.library.v1.Library.CreateShelf` and
 *  `google.example.library.v1.Library.CreateBook`, they will be renamed to
 *  `google.example.library.Library.CreateShelf` and
 *  `google.example.library.Library.CreateBook`. It essentially renames the
 *  proto package name section of the matched proto service and methods.
 */
@property(nonatomic, copy, nullable) NSString *renameTo;

/**
 *  Selects the methods to which this rule applies.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  Billing related configuration of the service.
 *  The following example shows how to configure monitored resources and metrics
 *  for billing:
 *  monitored_resources:
 *  - type: library.googleapis.com/branch
 *  labels:
 *  - key: /city
 *  description: The city where the library branch is located in.
 *  - key: /name
 *  description: The name of the branch.
 *  metrics:
 *  - name: library.googleapis.com/book/borrowed_count
 *  metric_kind: DELTA
 *  value_type: INT64
 *  billing:
 *  consumer_destinations:
 *  - monitored_resource: library.googleapis.com/branch
 *  metrics:
 *  - library.googleapis.com/book/borrowed_count
 */
@interface GTLRServiceManagement_Billing : GTLRObject

/**
 *  Billing configurations for sending metrics to the consumer project.
 *  There can be multiple consumer destinations per service, each one must have
 *  a different monitored resource type. A metric can be used in at most
 *  one consumer destination.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_BillingDestination *> *consumerDestinations;

@end


/**
 *  Configuration of a specific billing destination (Currently only support
 *  bill against consumer project).
 */
@interface GTLRServiceManagement_BillingDestination : GTLRObject

/**
 *  Names of the metrics to report to this billing destination.
 *  Each name must be defined in Service.metrics section.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *metrics;

/**
 *  The monitored resource type. The type must be defined in
 *  Service.monitored_resources section.
 */
@property(nonatomic, copy, nullable) NSString *monitoredResource;

@end


/**
 *  Associates `members` with a `role`.
 */
@interface GTLRServiceManagement_Binding : GTLRObject

/**
 *  The condition that is associated with this binding.
 *  NOTE: An unsatisfied condition will not allow user access via current
 *  binding. Different bindings, including their conditions, are examined
 *  independently.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Expr *condition;

/**
 *  Specifies the identities requesting access for a Cloud Platform resource.
 *  `members` can have the following values:
 *  * `allUsers`: A special identifier that represents anyone who is
 *  on the internet; with or without a Google account.
 *  * `allAuthenticatedUsers`: A special identifier that represents anyone
 *  who is authenticated with a Google account or a service account.
 *  * `user:{emailid}`: An email address that represents a specific Google
 *  account. For example, `alice\@example.com` .
 *  * `serviceAccount:{emailid}`: An email address that represents a service
 *  account. For example, `my-other-app\@appspot.gserviceaccount.com`.
 *  * `group:{emailid}`: An email address that represents a Google group.
 *  For example, `admins\@example.com`.
 *  * `deleted:user:{emailid}?uid={uniqueid}`: An email address (plus unique
 *  identifier) representing a user that has been recently deleted. For
 *  example, `alice\@example.com?uid=123456789012345678901`. If the user is
 *  recovered, this value reverts to `user:{emailid}` and the recovered user
 *  retains the role in the binding.
 *  * `deleted:serviceAccount:{emailid}?uid={uniqueid}`: An email address (plus
 *  unique identifier) representing a service account that has been recently
 *  deleted. For example,
 *  `my-other-app\@appspot.gserviceaccount.com?uid=123456789012345678901`.
 *  If the service account is undeleted, this value reverts to
 *  `serviceAccount:{emailid}` and the undeleted service account retains the
 *  role in the binding.
 *  * `deleted:group:{emailid}?uid={uniqueid}`: An email address (plus unique
 *  identifier) representing a Google group that has been recently
 *  deleted. For example, `admins\@example.com?uid=123456789012345678901`. If
 *  the group is recovered, this value reverts to `group:{emailid}` and the
 *  recovered group retains the role in the binding.
 *  * `domain:{domain}`: The G Suite domain (primary) that represents all the
 *  users of that domain. For example, `google.com` or `example.com`.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *members;

/**
 *  Role that is assigned to `members`.
 *  For example, `roles/viewer`, `roles/editor`, or `roles/owner`.
 */
@property(nonatomic, copy, nullable) NSString *role;

@end


/**
 *  Change report associated with a particular service configuration.
 *  It contains a list of ConfigChanges based on the comparison between
 *  two service configurations.
 */
@interface GTLRServiceManagement_ChangeReport : GTLRObject

/**
 *  List of changes between two service configurations.
 *  The changes will be alphabetically sorted based on the identifier
 *  of each change.
 *  A ConfigChange identifier is a dot separated path to the configuration.
 *  Example: visibility.rules[selector='LibraryService.CreateBook'].restriction
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_ConfigChange *> *configChanges;

@end


/**
 *  Output generated from semantically comparing two versions of a service
 *  configuration.
 *  Includes detailed information about a field that have changed with
 *  applicable advice about potential consequences for the change, such as
 *  backwards-incompatibility.
 */
@interface GTLRServiceManagement_ConfigChange : GTLRObject

/**
 *  Collection of advice provided for this change, useful for determining the
 *  possible impact of this change.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Advice *> *advices;

/**
 *  The type for this change, either ADDED, REMOVED, or MODIFIED.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_ConfigChange_ChangeType_Added The changed
 *        object exists in the 'new' service configuration, but not
 *        in the 'old' service configuration. (Value: "ADDED")
 *    @arg @c kGTLRServiceManagement_ConfigChange_ChangeType_ChangeTypeUnspecified
 *        No value was provided. (Value: "CHANGE_TYPE_UNSPECIFIED")
 *    @arg @c kGTLRServiceManagement_ConfigChange_ChangeType_Modified The
 *        changed object exists in both service configurations, but its value
 *        is different. (Value: "MODIFIED")
 *    @arg @c kGTLRServiceManagement_ConfigChange_ChangeType_Removed The changed
 *        object exists in the 'old' service configuration, but not
 *        in the 'new' service configuration. (Value: "REMOVED")
 */
@property(nonatomic, copy, nullable) NSString *changeType;

/**
 *  Object hierarchy path to the change, with levels separated by a '.'
 *  character. For repeated fields, an applicable unique identifier field is
 *  used for the index (usually selector, name, or id). For maps, the term
 *  'key' is used. If the field has no unique identifier, the numeric index
 *  is used.
 *  Examples:
 *  - visibility.rules[selector=="google.LibraryService.ListBooks"].restriction
 *  - quota.metric_rules[selector=="google"].metric_costs[key=="reads"].value
 *  - logging.producer_destinations[0]
 */
@property(nonatomic, copy, nullable) NSString *element;

/**
 *  Value of the changed object in the new Service configuration,
 *  in JSON format. This field will not be populated if ChangeType == REMOVED.
 */
@property(nonatomic, copy, nullable) NSString *newValue NS_RETURNS_NOT_RETAINED;

/**
 *  Value of the changed object in the old Service configuration,
 *  in JSON format. This field will not be populated if ChangeType == ADDED.
 */
@property(nonatomic, copy, nullable) NSString *oldValue;

@end


/**
 *  Generic specification of a source configuration file
 */
@interface GTLRServiceManagement_ConfigFile : GTLRObject

/**
 *  The bytes that constitute the file.
 *
 *  Contains encoded binary data; GTLRBase64 can encode/decode (probably
 *  web-safe format).
 */
@property(nonatomic, copy, nullable) NSString *fileContents;

/** The file name of the configuration file (full or relative path). */
@property(nonatomic, copy, nullable) NSString *filePath;

/**
 *  The type of configuration file this represents.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_ConfigFile_FileType_FileDescriptorSetProto
 *        FileDescriptorSet, generated by protoc.
 *        To generate, use protoc with imports and source info included.
 *        For an example test.proto file, the following command would put the
 *        value
 *        in a new file named out.pb.
 *        $protoc --include_imports --include_source_info test.proto -o out.pb
 *        (Value: "FILE_DESCRIPTOR_SET_PROTO")
 *    @arg @c kGTLRServiceManagement_ConfigFile_FileType_FileTypeUnspecified
 *        Unknown file type. (Value: "FILE_TYPE_UNSPECIFIED")
 *    @arg @c kGTLRServiceManagement_ConfigFile_FileType_OpenApiJson OpenAPI
 *        specification, serialized in JSON. (Value: "OPEN_API_JSON")
 *    @arg @c kGTLRServiceManagement_ConfigFile_FileType_OpenApiYaml OpenAPI
 *        specification, serialized in YAML. (Value: "OPEN_API_YAML")
 *    @arg @c kGTLRServiceManagement_ConfigFile_FileType_ProtoFile Uncompiled
 *        Proto file. Used for storage and display purposes only,
 *        currently server-side compilation is not supported. Should match the
 *        inputs to 'protoc' command used to generated
 *        FILE_DESCRIPTOR_SET_PROTO. A
 *        file of this type can only be included if at least one file of type
 *        FILE_DESCRIPTOR_SET_PROTO is included. (Value: "PROTO_FILE")
 *    @arg @c kGTLRServiceManagement_ConfigFile_FileType_ServiceConfigYaml
 *        YAML-specification of service. (Value: "SERVICE_CONFIG_YAML")
 */
@property(nonatomic, copy, nullable) NSString *fileType;

@end


/**
 *  Represents a service configuration with its name and id.
 */
@interface GTLRServiceManagement_ConfigRef : GTLRObject

/**
 *  Resource name of a service config. It must have the following
 *  format: "services/{service name}/configs/{config id}".
 */
@property(nonatomic, copy, nullable) NSString *name;

@end


/**
 *  Represents a source file which is used to generate the service configuration
 *  defined by `google.api.Service`.
 */
@interface GTLRServiceManagement_ConfigSource : GTLRObject

/**
 *  Set of source configuration files that are used to generate a service
 *  configuration (`google.api.Service`).
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_ConfigFile *> *files;

/**
 *  A unique ID for a specific instance of this message, typically assigned
 *  by the client for tracking purpose. If empty, the server may choose to
 *  generate one instead.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 */
@property(nonatomic, copy, nullable) NSString *identifier;

@end


/**
 *  `Context` defines which contexts an API requests.
 *  Example:
 *  context:
 *  rules:
 *  - selector: "*"
 *  requested:
 *  - google.rpc.context.ProjectContext
 *  - google.rpc.context.OriginContext
 *  The above specifies that all methods in the API request
 *  `google.rpc.context.ProjectContext` and
 *  `google.rpc.context.OriginContext`.
 *  Available context types are defined in package
 *  `google.rpc.context`.
 *  This also provides mechanism to whitelist any protobuf message extension
 *  that
 *  can be sent in grpc metadata using “x-goog-ext-<extension_id>-bin” and
 *  “x-goog-ext-<extension_id>-jspb” format. For example, list any service
 *  specific protobuf types that can appear in grpc metadata as follows in your
 *  yaml file:
 *  Example:
 *  context:
 *  rules:
 *  - selector: "google.example.library.v1.LibraryService.CreateBook"
 *  allowed_request_extensions:
 *  - google.foo.v1.NewExtension
 *  allowed_response_extensions:
 *  - google.foo.v1.NewExtension
 *  You can also specify extension ID instead of fully qualified extension name
 *  here.
 */
@interface GTLRServiceManagement_Context : GTLRObject

/**
 *  A list of RPC context rules that apply to individual API methods.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_ContextRule *> *rules;

@end


/**
 *  A context rule provides information about the context for an individual API
 *  element.
 */
@interface GTLRServiceManagement_ContextRule : GTLRObject

/**
 *  A list of full type names or extension IDs of extensions allowed in grpc
 *  side channel from client to backend.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *allowedRequestExtensions;

/**
 *  A list of full type names or extension IDs of extensions allowed in grpc
 *  side channel from backend to client.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *allowedResponseExtensions;

/** A list of full type names of provided contexts. */
@property(nonatomic, strong, nullable) NSArray<NSString *> *provided;

/** A list of full type names of requested contexts. */
@property(nonatomic, strong, nullable) NSArray<NSString *> *requested;

/**
 *  Selects the methods to which this rule applies.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  Selects and configures the service controller used by the service. The
 *  service controller handles features like abuse, quota, billing, logging,
 *  monitoring, etc.
 */
@interface GTLRServiceManagement_Control : GTLRObject

/**
 *  The service control environment to use. If empty, no control plane
 *  feature (like quota and billing) will be enabled.
 */
@property(nonatomic, copy, nullable) NSString *environment;

@end


/**
 *  Customize service error responses. For example, list any service
 *  specific protobuf types that can appear in error detail lists of
 *  error responses.
 *  Example:
 *  custom_error:
 *  types:
 *  - google.foo.v1.CustomError
 *  - google.foo.v1.AnotherError
 */
@interface GTLRServiceManagement_CustomError : GTLRObject

/**
 *  The list of custom error rules that apply to individual API messages.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_CustomErrorRule *> *rules;

/**
 *  The list of custom error detail types, e.g. 'google.foo.v1.CustomError'.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *types;

@end


/**
 *  A custom error rule.
 */
@interface GTLRServiceManagement_CustomErrorRule : GTLRObject

/**
 *  Mark this message as possible payload in error response. Otherwise,
 *  objects of this type will be filtered when they appear in error payload.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *isErrorType;

/**
 *  Selects messages to which this rule applies.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  A custom pattern is used for defining custom HTTP verb.
 */
@interface GTLRServiceManagement_CustomHttpPattern : GTLRObject

/** The name of this custom HTTP verb. */
@property(nonatomic, copy, nullable) NSString *kind;

/** The path matched by this custom verb. */
@property(nonatomic, copy, nullable) NSString *path;

@end


/**
 *  Strategy used to delete a service. This strategy is a placeholder only
 *  used by the system generated rollout to delete a service.
 */
@interface GTLRServiceManagement_DeleteServiceStrategy : GTLRObject
@end


/**
 *  Represents a diagnostic message (error or warning)
 */
@interface GTLRServiceManagement_Diagnostic : GTLRObject

/**
 *  The kind of diagnostic information provided.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Diagnostic_Kind_Error Only errors (Value:
 *        "ERROR")
 *    @arg @c kGTLRServiceManagement_Diagnostic_Kind_Warning Warnings and errors
 *        (Value: "WARNING")
 */
@property(nonatomic, copy, nullable) NSString *kind;

/** File name and line number of the error or warning. */
@property(nonatomic, copy, nullable) NSString *location;

/** Message describing the error or warning. */
@property(nonatomic, copy, nullable) NSString *message;

@end


/**
 *  Request message for DisableService method.
 */
@interface GTLRServiceManagement_DisableServiceRequest : GTLRObject

/**
 *  Required. The identity of consumer resource which service disablement will
 *  be
 *  applied to.
 *  The Google Service Management implementation accepts the following
 *  forms:
 *  - "project:<project_id>"
 *  Note: this is made compatible with
 *  google.api.servicecontrol.v1.Operation.consumer_id.
 */
@property(nonatomic, copy, nullable) NSString *consumerId;

@end


/**
 *  Operation payload for DisableService method.
 */
@interface GTLRServiceManagement_DisableServiceResponse : GTLRObject
@end


/**
 *  `Documentation` provides the information for describing a service.
 *  Example:
 *  <pre><code>documentation:
 *  summary: >
 *  The Google Calendar API gives access
 *  to most calendar features.
 *  pages:
 *  - name: Overview
 *  content: &#40;== include google/foo/overview.md ==&#41;
 *  - name: Tutorial
 *  content: &#40;== include google/foo/tutorial.md ==&#41;
 *  subpages;
 *  - name: Java
 *  content: &#40;== include google/foo/tutorial_java.md ==&#41;
 *  rules:
 *  - selector: google.calendar.Calendar.Get
 *  description: >
 *  ...
 *  - selector: google.calendar.Calendar.Put
 *  description: >
 *  ...
 *  </code></pre>
 *  Documentation is provided in markdown syntax. In addition to
 *  standard markdown features, definition lists, tables and fenced
 *  code blocks are supported. Section headers can be provided and are
 *  interpreted relative to the section nesting of the context where
 *  a documentation fragment is embedded.
 *  Documentation from the IDL is merged with documentation defined
 *  via the config at normalization time, where documentation provided
 *  by config rules overrides IDL provided.
 *  A number of constructs specific to the API platform are supported
 *  in documentation text.
 *  In order to reference a proto element, the following
 *  notation can be used:
 *  <pre><code>&#91;fully.qualified.proto.name]&#91;]</code></pre>
 *  To override the display text used for the link, this can be used:
 *  <pre><code>&#91;display text]&#91;fully.qualified.proto.name]</code></pre>
 *  Text can be excluded from doc using the following notation:
 *  <pre><code>&#40;-- internal comment --&#41;</code></pre>
 *  A few directives are available in documentation. Note that
 *  directives must appear on a single line to be properly
 *  identified. The `include` directive includes a markdown file from
 *  an external source:
 *  <pre><code>&#40;== include path/to/file ==&#41;</code></pre>
 *  The `resource_for` directive marks a message to be the resource of
 *  a collection in REST view. If it is not specified, tools attempt
 *  to infer the resource from the operations in a collection:
 *  <pre><code>&#40;== resource_for v1.shelves.books ==&#41;</code></pre>
 *  The directive `suppress_warning` does not directly affect documentation
 *  and is documented together with service config validation.
 */
@interface GTLRServiceManagement_Documentation : GTLRObject

/** The URL to the root of documentation. */
@property(nonatomic, copy, nullable) NSString *documentationRootUrl;

/**
 *  Declares a single overview page. For example:
 *  <pre><code>documentation:
 *  summary: ...
 *  overview: &#40;== include overview.md ==&#41;
 *  </code></pre>
 *  This is a shortcut for the following declaration (using pages style):
 *  <pre><code>documentation:
 *  summary: ...
 *  pages:
 *  - name: Overview
 *  content: &#40;== include overview.md ==&#41;
 *  </code></pre>
 *  Note: you cannot specify both `overview` field and `pages` field.
 */
@property(nonatomic, copy, nullable) NSString *overview;

/** The top level pages for the documentation set. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Page *> *pages;

/**
 *  A list of documentation rules that apply to individual API elements.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_DocumentationRule *> *rules;

/**
 *  Specifies the service root url if the default one (the service name
 *  from the yaml file) is not suitable. This can be seen in any fully
 *  specified service urls as well as sections that show a base that other
 *  urls are relative to.
 */
@property(nonatomic, copy, nullable) NSString *serviceRootUrl;

/**
 *  A short summary of what the service does. Can only be provided by
 *  plain text.
 */
@property(nonatomic, copy, nullable) NSString *summary;

@end


/**
 *  A documentation rule provides information about individual API elements.
 */
@interface GTLRServiceManagement_DocumentationRule : GTLRObject

/**
 *  Deprecation description of the selected element(s). It can be provided if
 *  an element is marked as `deprecated`.
 */
@property(nonatomic, copy, nullable) NSString *deprecationDescription;

/**
 *  Description of the selected API(s).
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  The selector is a comma-separated list of patterns. Each pattern is a
 *  qualified name of the element which may end in "*", indicating a wildcard.
 *  Wildcards are only allowed at the end and for a whole component of the
 *  qualified name, i.e. "foo.*" is ok, but not "foo.b*" or "foo.*.bar". A
 *  wildcard will match one or more components. To specify a default for all
 *  applicable elements, the whole pattern "*" is used.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  Request message for EnableService method.
 */
@interface GTLRServiceManagement_EnableServiceRequest : GTLRObject

/**
 *  Required. The identity of consumer resource which service enablement will be
 *  applied to.
 *  The Google Service Management implementation accepts the following
 *  forms:
 *  - "project:<project_id>"
 *  Note: this is made compatible with
 *  google.api.servicecontrol.v1.Operation.consumer_id.
 */
@property(nonatomic, copy, nullable) NSString *consumerId;

@end


/**
 *  Operation payload for EnableService method.
 */
@interface GTLRServiceManagement_EnableServiceResponse : GTLRObject
@end


/**
 *  `Endpoint` describes a network endpoint that serves a set of APIs.
 *  A service may expose any number of endpoints, and all endpoints share the
 *  same service configuration, such as quota configuration and monitoring
 *  configuration.
 *  Example service configuration:
 *  name: library-example.googleapis.com
 *  endpoints:
 *  # Below entry makes 'google.example.library.v1.Library'
 *  # API be served from endpoint address library-example.googleapis.com.
 *  # It also allows HTTP OPTIONS calls to be passed to the backend, for
 *  # it to decide whether the subsequent cross-origin request is
 *  # allowed to proceed.
 *  - name: library-example.googleapis.com
 *  allow_cors: true
 */
@interface GTLRServiceManagement_Endpoint : GTLRObject

/**
 *  DEPRECATED: This field is no longer supported. Instead of using aliases,
 *  please specify multiple google.api.Endpoint for each of the intended
 *  aliases.
 *  Additional names that this endpoint will be hosted on.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *aliases;

/**
 *  Allowing
 *  [CORS](https://en.wikipedia.org/wiki/Cross-origin_resource_sharing), aka
 *  cross-domain traffic, would allow the backends served from this endpoint to
 *  receive and respond to HTTP OPTIONS requests. The response will be used by
 *  the browser to determine whether the subsequent cross-origin request is
 *  allowed to proceed.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *allowCors;

/** The list of features enabled on this endpoint. */
@property(nonatomic, strong, nullable) NSArray<NSString *> *features;

/** The canonical name of this endpoint. */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  The specification of an Internet routable address of API frontend that will
 *  handle requests to this [API
 *  Endpoint](https://cloud.google.com/apis/design/glossary). It should be
 *  either a valid IPv4 address or a fully-qualified domain name. For example,
 *  "8.8.8.8" or "myservice.appspot.com".
 */
@property(nonatomic, copy, nullable) NSString *target;

@end


/**
 *  Enum type definition.
 */
@interface GTLRServiceManagement_Enum : GTLRObject

/** Enum value definitions. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_EnumValue *> *enumvalue;

/** Enum type name. */
@property(nonatomic, copy, nullable) NSString *name;

/** Protocol buffer options. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Option *> *options;

/** The source context. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_SourceContext *sourceContext;

/**
 *  The source syntax.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Enum_Syntax_SyntaxProto2 Syntax `proto2`.
 *        (Value: "SYNTAX_PROTO2")
 *    @arg @c kGTLRServiceManagement_Enum_Syntax_SyntaxProto3 Syntax `proto3`.
 *        (Value: "SYNTAX_PROTO3")
 */
@property(nonatomic, copy, nullable) NSString *syntax;

@end


/**
 *  Enum value definition.
 */
@interface GTLRServiceManagement_EnumValue : GTLRObject

/** Enum value name. */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  Enum value number.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *number;

/** Protocol buffer options. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Option *> *options;

@end


/**
 *  Represents a textual expression in the Common Expression Language (CEL)
 *  syntax. CEL is a C-like expression language. The syntax and semantics of CEL
 *  are documented at https://github.com/google/cel-spec.
 *  Example (Comparison):
 *  title: "Summary size limit"
 *  description: "Determines if a summary is less than 100 chars"
 *  expression: "document.summary.size() < 100"
 *  Example (Equality):
 *  title: "Requestor is owner"
 *  description: "Determines if requestor is the document owner"
 *  expression: "document.owner == request.auth.claims.email"
 *  Example (Logic):
 *  title: "Public documents"
 *  description: "Determine whether the document should be publicly visible"
 *  expression: "document.type != 'private' && document.type != 'internal'"
 *  Example (Data Manipulation):
 *  title: "Notification string"
 *  description: "Create a notification string with a timestamp."
 *  expression: "'New message received at ' + string(document.create_time)"
 *  The exact variables and functions that may be referenced within an
 *  expression
 *  are determined by the service that evaluates it. See the service
 *  documentation for additional information.
 */
@interface GTLRServiceManagement_Expr : GTLRObject

/**
 *  Optional. Description of the expression. This is a longer text which
 *  describes the expression, e.g. when hovered over it in a UI.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  Textual representation of an expression in Common Expression Language
 *  syntax.
 */
@property(nonatomic, copy, nullable) NSString *expression;

/**
 *  Optional. String indicating the location of the expression for error
 *  reporting, e.g. a file name and a position in the file.
 */
@property(nonatomic, copy, nullable) NSString *location;

/**
 *  Optional. Title for the expression, i.e. a short string describing
 *  its purpose. This can be used e.g. in UIs which allow to enter the
 *  expression.
 */
@property(nonatomic, copy, nullable) NSString *title;

@end


/**
 *  A single field of a message type.
 */
@interface GTLRServiceManagement_Field : GTLRObject

/**
 *  The field cardinality.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Field_Cardinality_CardinalityOptional For
 *        optional fields. (Value: "CARDINALITY_OPTIONAL")
 *    @arg @c kGTLRServiceManagement_Field_Cardinality_CardinalityRepeated For
 *        repeated fields. (Value: "CARDINALITY_REPEATED")
 *    @arg @c kGTLRServiceManagement_Field_Cardinality_CardinalityRequired For
 *        required fields. Proto2 syntax only. (Value: "CARDINALITY_REQUIRED")
 *    @arg @c kGTLRServiceManagement_Field_Cardinality_CardinalityUnknown For
 *        fields with unknown cardinality. (Value: "CARDINALITY_UNKNOWN")
 */
@property(nonatomic, copy, nullable) NSString *cardinality;

/**
 *  The string value of the default value of this field. Proto2 syntax only.
 */
@property(nonatomic, copy, nullable) NSString *defaultValue;

/** The field JSON name. */
@property(nonatomic, copy, nullable) NSString *jsonName;

/**
 *  The field type.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeBool Field type bool.
 *        (Value: "TYPE_BOOL")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeBytes Field type bytes.
 *        (Value: "TYPE_BYTES")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeDouble Field type double.
 *        (Value: "TYPE_DOUBLE")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeEnum Field type enum.
 *        (Value: "TYPE_ENUM")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeFixed32 Field type fixed32.
 *        (Value: "TYPE_FIXED32")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeFixed64 Field type fixed64.
 *        (Value: "TYPE_FIXED64")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeFloat Field type float.
 *        (Value: "TYPE_FLOAT")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeGroup Field type group.
 *        Proto2 syntax only, and deprecated. (Value: "TYPE_GROUP")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeInt32 Field type int32.
 *        (Value: "TYPE_INT32")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeInt64 Field type int64.
 *        (Value: "TYPE_INT64")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeMessage Field type message.
 *        (Value: "TYPE_MESSAGE")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeSfixed32 Field type
 *        sfixed32. (Value: "TYPE_SFIXED32")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeSfixed64 Field type
 *        sfixed64. (Value: "TYPE_SFIXED64")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeSint32 Field type sint32.
 *        (Value: "TYPE_SINT32")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeSint64 Field type sint64.
 *        (Value: "TYPE_SINT64")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeString Field type string.
 *        (Value: "TYPE_STRING")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeUint32 Field type uint32.
 *        (Value: "TYPE_UINT32")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeUint64 Field type uint64.
 *        (Value: "TYPE_UINT64")
 *    @arg @c kGTLRServiceManagement_Field_Kind_TypeUnknown Field type unknown.
 *        (Value: "TYPE_UNKNOWN")
 */
@property(nonatomic, copy, nullable) NSString *kind;

/** The field name. */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  The field number.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *number;

/**
 *  The index of the field type in `Type.oneofs`, for message or enumeration
 *  types. The first type has index 1; zero means the type is not in the list.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *oneofIndex;

/** The protocol buffer options. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Option *> *options;

/**
 *  Whether to use alternative packed wire representation.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *packed;

/**
 *  The field type URL, without the scheme, for message or enumeration
 *  types. Example: `"type.googleapis.com/google.protobuf.Timestamp"`.
 */
@property(nonatomic, copy, nullable) NSString *typeUrl;

@end


/**
 *  Encapsulation of flow-specific error details for debugging.
 *  Used as a details field on an error Status, not intended for external use.
 */
@interface GTLRServiceManagement_FlowErrorDetails : GTLRObject

/** The type of exception (as a class name). */
@property(nonatomic, copy, nullable) NSString *exceptionType;

/** The step that failed. */
@property(nonatomic, copy, nullable) NSString *flowStepId;

@end


/**
 *  Request message for GenerateConfigReport method.
 */
@interface GTLRServiceManagement_GenerateConfigReportRequest : GTLRObject

/**
 *  Required. Service configuration for which we want to generate the report.
 *  For this version of API, the supported types are
 *  google.api.servicemanagement.v1.ConfigRef,
 *  google.api.servicemanagement.v1.ConfigSource,
 *  and google.api.Service
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_GenerateConfigReportRequest_NewConfig *newConfig NS_RETURNS_NOT_RETAINED;

/**
 *  Optional. Service configuration against which the comparison will be done.
 *  For this version of API, the supported types are
 *  google.api.servicemanagement.v1.ConfigRef,
 *  google.api.servicemanagement.v1.ConfigSource,
 *  and google.api.Service
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_GenerateConfigReportRequest_OldConfig *oldConfig;

@end


/**
 *  Required. Service configuration for which we want to generate the report.
 *  For this version of API, the supported types are
 *  google.api.servicemanagement.v1.ConfigRef,
 *  google.api.servicemanagement.v1.ConfigSource,
 *  and google.api.Service
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_GenerateConfigReportRequest_NewConfig : GTLRObject
@end


/**
 *  Optional. Service configuration against which the comparison will be done.
 *  For this version of API, the supported types are
 *  google.api.servicemanagement.v1.ConfigRef,
 *  google.api.servicemanagement.v1.ConfigSource,
 *  and google.api.Service
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_GenerateConfigReportRequest_OldConfig : GTLRObject
@end


/**
 *  Response message for GenerateConfigReport method.
 */
@interface GTLRServiceManagement_GenerateConfigReportResponse : GTLRObject

/**
 *  list of ChangeReport, each corresponding to comparison between two
 *  service configurations.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_ChangeReport *> *changeReports;

/**
 *  Errors / Linter warnings associated with the service definition this
 *  report
 *  belongs to.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Diagnostic *> *diagnostics;

/**
 *  ID of the service configuration this report belongs to.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 */
@property(nonatomic, copy, nullable) NSString *identifier;

/** Name of the service this report belongs to. */
@property(nonatomic, copy, nullable) NSString *serviceName;

@end


/**
 *  Request message for `GetIamPolicy` method.
 */
@interface GTLRServiceManagement_GetIamPolicyRequest : GTLRObject

/**
 *  OPTIONAL: A `GetPolicyOptions` object for specifying options to
 *  `GetIamPolicy`. This field is only used by Cloud IAM.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_GetPolicyOptions *options;

@end


/**
 *  Encapsulates settings provided to GetIamPolicy.
 */
@interface GTLRServiceManagement_GetPolicyOptions : GTLRObject

/**
 *  Optional. The policy format version to be returned.
 *  Valid values are 0, 1, and 3. Requests specifying an invalid value will be
 *  rejected.
 *  Requests for policies with any conditional bindings must specify version 3.
 *  Policies without any conditional bindings may specify any valid value or
 *  leave the field unset.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *requestedPolicyVersion;

@end


/**
 *  Defines the HTTP configuration for an API service. It contains a list of
 *  HttpRule, each specifying the mapping of an RPC method
 *  to one or more HTTP REST API methods.
 */
@interface GTLRServiceManagement_Http : GTLRObject

/**
 *  When set to true, URL path parameters will be fully URI-decoded except in
 *  cases of single segment matches in reserved expansion, where "%2F" will be
 *  left encoded.
 *  The default behavior is to not decode RFC 6570 reserved characters in multi
 *  segment matches.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *fullyDecodeReservedExpansion;

/**
 *  A list of HTTP configuration rules that apply to individual API methods.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_HttpRule *> *rules;

@end


/**
 *  # gRPC Transcoding
 *  gRPC Transcoding is a feature for mapping between a gRPC method and one or
 *  more HTTP REST endpoints. It allows developers to build a single API service
 *  that supports both gRPC APIs and REST APIs. Many systems, including [Google
 *  APIs](https://github.com/googleapis/googleapis),
 *  [Cloud Endpoints](https://cloud.google.com/endpoints), [gRPC
 *  Gateway](https://github.com/grpc-ecosystem/grpc-gateway),
 *  and [Envoy](https://github.com/envoyproxy/envoy) proxy support this feature
 *  and use it for large scale production services.
 *  `HttpRule` defines the schema of the gRPC/REST mapping. The mapping
 *  specifies
 *  how different portions of the gRPC request message are mapped to the URL
 *  path, URL query parameters, and HTTP request body. It also controls how the
 *  gRPC response message is mapped to the HTTP response body. `HttpRule` is
 *  typically specified as an `google.api.http` annotation on the gRPC method.
 *  Each mapping specifies a URL path template and an HTTP method. The path
 *  template may refer to one or more fields in the gRPC request message, as
 *  long
 *  as each field is a non-repeated field with a primitive (non-message) type.
 *  The path template controls how fields of the request message are mapped to
 *  the URL path.
 *  Example:
 *  service Messaging {
 *  rpc GetMessage(GetMessageRequest) returns (Message) {
 *  option (google.api.http) = {
 *  get: "/v1/{name=messages/ *}"
 *  };
 *  }
 *  }
 *  message GetMessageRequest {
 *  string name = 1; // Mapped to URL path.
 *  }
 *  message Message {
 *  string text = 1; // The resource content.
 *  }
 *  This enables an HTTP REST to gRPC mapping as below:
 *  HTTP | gRPC
 *  -----|-----
 *  `GET /v1/messages/123456` | `GetMessage(name: "messages/123456")`
 *  Any fields in the request message which are not bound by the path template
 *  automatically become HTTP query parameters if there is no HTTP request body.
 *  For example:
 *  service Messaging {
 *  rpc GetMessage(GetMessageRequest) returns (Message) {
 *  option (google.api.http) = {
 *  get:"/v1/messages/{message_id}"
 *  };
 *  }
 *  }
 *  message GetMessageRequest {
 *  message SubMessage {
 *  string subfield = 1;
 *  }
 *  string message_id = 1; // Mapped to URL path.
 *  int64 revision = 2; // Mapped to URL query parameter `revision`.
 *  SubMessage sub = 3; // Mapped to URL query parameter `sub.subfield`.
 *  }
 *  This enables a HTTP JSON to RPC mapping as below:
 *  HTTP | gRPC
 *  -----|-----
 *  `GET /v1/messages/123456?revision=2&sub.subfield=foo` |
 *  `GetMessage(message_id: "123456" revision: 2 sub: SubMessage(subfield:
 *  "foo"))`
 *  Note that fields which are mapped to URL query parameters must have a
 *  primitive type or a repeated primitive type or a non-repeated message type.
 *  In the case of a repeated type, the parameter can be repeated in the URL
 *  as `...?param=A&param=B`. In the case of a message type, each field of the
 *  message is mapped to a separate parameter, such as
 *  `...?foo.a=A&foo.b=B&foo.c=C`.
 *  For HTTP methods that allow a request body, the `body` field
 *  specifies the mapping. Consider a REST update method on the
 *  message resource collection:
 *  service Messaging {
 *  rpc UpdateMessage(UpdateMessageRequest) returns (Message) {
 *  option (google.api.http) = {
 *  patch: "/v1/messages/{message_id}"
 *  body: "message"
 *  };
 *  }
 *  }
 *  message UpdateMessageRequest {
 *  string message_id = 1; // mapped to the URL
 *  Message message = 2; // mapped to the body
 *  }
 *  The following HTTP JSON to RPC mapping is enabled, where the
 *  representation of the JSON in the request body is determined by
 *  protos JSON encoding:
 *  HTTP | gRPC
 *  -----|-----
 *  `PATCH /v1/messages/123456 { "text": "Hi!" }` | `UpdateMessage(message_id:
 *  "123456" message { text: "Hi!" })`
 *  The special name `*` can be used in the body mapping to define that
 *  every field not bound by the path template should be mapped to the
 *  request body. This enables the following alternative definition of
 *  the update method:
 *  service Messaging {
 *  rpc UpdateMessage(Message) returns (Message) {
 *  option (google.api.http) = {
 *  patch: "/v1/messages/{message_id}"
 *  body: "*"
 *  };
 *  }
 *  }
 *  message Message {
 *  string message_id = 1;
 *  string text = 2;
 *  }
 *  The following HTTP JSON to RPC mapping is enabled:
 *  HTTP | gRPC
 *  -----|-----
 *  `PATCH /v1/messages/123456 { "text": "Hi!" }` | `UpdateMessage(message_id:
 *  "123456" text: "Hi!")`
 *  Note that when using `*` in the body mapping, it is not possible to
 *  have HTTP parameters, as all fields not bound by the path end in
 *  the body. This makes this option more rarely used in practice when
 *  defining REST APIs. The common usage of `*` is in custom methods
 *  which don't use the URL at all for transferring data.
 *  It is possible to define multiple HTTP methods for one RPC by using
 *  the `additional_bindings` option. Example:
 *  service Messaging {
 *  rpc GetMessage(GetMessageRequest) returns (Message) {
 *  option (google.api.http) = {
 *  get: "/v1/messages/{message_id}"
 *  additional_bindings {
 *  get: "/v1/users/{user_id}/messages/{message_id}"
 *  }
 *  };
 *  }
 *  }
 *  message GetMessageRequest {
 *  string message_id = 1;
 *  string user_id = 2;
 *  }
 *  This enables the following two alternative HTTP JSON to RPC mappings:
 *  HTTP | gRPC
 *  -----|-----
 *  `GET /v1/messages/123456` | `GetMessage(message_id: "123456")`
 *  `GET /v1/users/me/messages/123456` | `GetMessage(user_id: "me" message_id:
 *  "123456")`
 *  ## Rules for HTTP mapping
 *  1. Leaf request fields (recursive expansion nested messages in the request
 *  message) are classified into three categories:
 *  - Fields referred by the path template. They are passed via the URL path.
 *  - Fields referred by the HttpRule.body. They are passed via the HTTP
 *  request body.
 *  - All other fields are passed via the URL query parameters, and the
 *  parameter name is the field path in the request message. A repeated
 *  field can be represented as multiple query parameters under the same
 *  name.
 *  2. If HttpRule.body is "*", there is no URL query parameter, all fields
 *  are passed via URL path and HTTP request body.
 *  3. If HttpRule.body is omitted, there is no HTTP request body, all
 *  fields are passed via URL path and URL query parameters.
 *  ### Path template syntax
 *  Template = "/" Segments [ Verb ] ;
 *  Segments = Segment { "/" Segment } ;
 *  Segment = "*" | "**" | LITERAL | Variable ;
 *  Variable = "{" FieldPath [ "=" Segments ] "}" ;
 *  FieldPath = IDENT { "." IDENT } ;
 *  Verb = ":" LITERAL ;
 *  The syntax `*` matches a single URL path segment. The syntax `**` matches
 *  zero or more URL path segments, which must be the last part of the URL path
 *  except the `Verb`.
 *  The syntax `Variable` matches part of the URL path as specified by its
 *  template. A variable template must not contain other variables. If a
 *  variable
 *  matches a single path segment, its template may be omitted, e.g. `{var}`
 *  is equivalent to `{var=*}`.
 *  The syntax `LITERAL` matches literal text in the URL path. If the `LITERAL`
 *  contains any reserved character, such characters should be percent-encoded
 *  before the matching.
 *  If a variable contains exactly one path segment, such as `"{var}"` or
 *  `"{var=*}"`, when such a variable is expanded into a URL path on the client
 *  side, all characters except `[-_.~0-9a-zA-Z]` are percent-encoded. The
 *  server side does the reverse decoding. Such variables show up in the
 *  [Discovery
 *  Document](https://developers.google.com/discovery/v1/reference/apis) as
 *  `{var}`.
 *  If a variable contains multiple path segments, such as `"{var=foo/ *}"`
 *  or `"{var=**}"`, when such a variable is expanded into a URL path on the
 *  client side, all characters except `[-_.~/0-9a-zA-Z]` are percent-encoded.
 *  The server side does the reverse decoding, except "%2F" and "%2f" are left
 *  unchanged. Such variables show up in the
 *  [Discovery
 *  Document](https://developers.google.com/discovery/v1/reference/apis) as
 *  `{+var}`.
 *  ## Using gRPC API Service Configuration
 *  gRPC API Service Configuration (service config) is a configuration language
 *  for configuring a gRPC service to become a user-facing product. The
 *  service config is simply the YAML representation of the `google.api.Service`
 *  proto message.
 *  As an alternative to annotating your proto file, you can configure gRPC
 *  transcoding in your service config YAML files. You do this by specifying a
 *  `HttpRule` that maps the gRPC method to a REST endpoint, achieving the same
 *  effect as the proto annotation. This can be particularly useful if you
 *  have a proto that is reused in multiple services. Note that any transcoding
 *  specified in the service config will override any matching transcoding
 *  configuration in the proto.
 *  Example:
 *  http:
 *  rules:
 *  # Selects a gRPC method and applies HttpRule to it.
 *  - selector: example.v1.Messaging.GetMessage
 *  get: /v1/messages/{message_id}/{sub.subfield}
 *  ## Special notes
 *  When gRPC Transcoding is used to map a gRPC to JSON REST endpoints, the
 *  proto to JSON conversion must follow the [proto3
 *  specification](https://developers.google.com/protocol-buffers/docs/proto3#json).
 *  While the single segment variable follows the semantics of
 *  [RFC 6570](https://tools.ietf.org/html/rfc6570) Section 3.2.2 Simple String
 *  Expansion, the multi segment variable **does not** follow RFC 6570 Section
 *  3.2.3 Reserved Expansion. The reason is that the Reserved Expansion
 *  does not expand special characters like `?` and `#`, which would lead
 *  to invalid URLs. As the result, gRPC Transcoding uses a custom encoding
 *  for multi segment variables.
 *  The path variables **must not** refer to any repeated or mapped field,
 *  because client libraries are not capable of handling such variable
 *  expansion.
 *  The path variables **must not** capture the leading "/" character. The
 *  reason
 *  is that the most common use case "{var}" does not capture the leading "/"
 *  character. For consistency, all path variables must share the same behavior.
 *  Repeated message fields must not be mapped to URL query parameters, because
 *  no client library can support such complicated mapping.
 *  If an API needs to use a JSON array for request or response body, it can map
 *  the request or response body to a repeated field. However, some gRPC
 *  Transcoding implementations may not support this feature.
 */
@interface GTLRServiceManagement_HttpRule : GTLRObject

/**
 *  Additional HTTP bindings for the selector. Nested bindings must
 *  not contain an `additional_bindings` field themselves (that is,
 *  the nesting may only be one level deep).
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_HttpRule *> *additionalBindings;

/**
 *  When this flag is set to true, HTTP requests will be allowed to invoke a
 *  half-duplex streaming method.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *allowHalfDuplex;

/**
 *  The name of the request field whose value is mapped to the HTTP request
 *  body, or `*` for mapping all request fields not captured by the path
 *  pattern to the HTTP body, or omitted for not having any HTTP request body.
 *  NOTE: the referred field must be present at the top-level of the request
 *  message type.
 */
@property(nonatomic, copy, nullable) NSString *body;

/**
 *  The custom pattern is used for specifying an HTTP method that is not
 *  included in the `pattern` field, such as HEAD, or "*" to leave the
 *  HTTP method unspecified for this rule. The wild-card rule is useful
 *  for services that provide content to Web (HTML) clients.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_CustomHttpPattern *custom;

/**
 *  Maps to HTTP DELETE. Used for deleting a resource.
 *
 *  Remapped to 'deleteProperty' to avoid language reserved word 'delete'.
 */
@property(nonatomic, copy, nullable) NSString *deleteProperty;

/**
 *  Maps to HTTP GET. Used for listing and getting information about
 *  resources.
 */
@property(nonatomic, copy, nullable) NSString *get;

/** Maps to HTTP PATCH. Used for updating a resource. */
@property(nonatomic, copy, nullable) NSString *patch;

/**
 *  Maps to HTTP POST. Used for creating a resource or performing an action.
 */
@property(nonatomic, copy, nullable) NSString *post;

/** Maps to HTTP PUT. Used for replacing a resource. */
@property(nonatomic, copy, nullable) NSString *put;

/**
 *  Optional. The name of the response field whose value is mapped to the HTTP
 *  response body. When omitted, the entire response message will be used
 *  as the HTTP response body.
 *  NOTE: The referred field must be present at the top-level of the response
 *  message type.
 */
@property(nonatomic, copy, nullable) NSString *responseBody;

/**
 *  Selects a method to which this rule applies.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  Specifies a location to extract JWT from an API request.
 */
@interface GTLRServiceManagement_JwtLocation : GTLRObject

/** Specifies HTTP header name to extract JWT token. */
@property(nonatomic, copy, nullable) NSString *header;

/** Specifies URL query parameter name to extract JWT token. */
@property(nonatomic, copy, nullable) NSString *query;

/**
 *  The value prefix. The value format is "value_prefix{token}"
 *  Only applies to "in" header type. Must be empty for "in" query type.
 *  If not empty, the header value has to match (case sensitive) this prefix.
 *  If not matched, JWT will not be extracted. If matched, JWT will be
 *  extracted after the prefix is removed.
 *  For example, for "Authorization: Bearer {JWT}",
 *  value_prefix="Bearer " with a space at the end.
 */
@property(nonatomic, copy, nullable) NSString *valuePrefix;

@end


/**
 *  A description of a label.
 */
@interface GTLRServiceManagement_LabelDescriptor : GTLRObject

/**
 *  A human-readable description for the label.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/** The label key. */
@property(nonatomic, copy, nullable) NSString *key;

/**
 *  The type of data that can be assigned to the label.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_LabelDescriptor_ValueType_Bool Boolean;
 *        true or false. (Value: "BOOL")
 *    @arg @c kGTLRServiceManagement_LabelDescriptor_ValueType_Int64 A 64-bit
 *        signed integer. (Value: "INT64")
 *    @arg @c kGTLRServiceManagement_LabelDescriptor_ValueType_String A
 *        variable-length string. This is the default. (Value: "STRING")
 */
@property(nonatomic, copy, nullable) NSString *valueType;

@end


/**
 *  The response message for Operations.ListOperations.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "operations" property. If returned as the result of a query, it
 *        should support automatic pagination (when @c shouldFetchNextPages is
 *        enabled).
 */
@interface GTLRServiceManagement_ListOperationsResponse : GTLRCollectionObject

/** The standard List next-page token. */
@property(nonatomic, copy, nullable) NSString *nextPageToken;

/**
 *  A list of operations that matches the specified filter in the request.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Operation *> *operations;

@end


/**
 *  Response message for ListServiceConfigs method.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "serviceConfigs" property. If returned as the result of a query,
 *        it should support automatic pagination (when @c shouldFetchNextPages
 *        is enabled).
 */
@interface GTLRServiceManagement_ListServiceConfigsResponse : GTLRCollectionObject

/** The token of the next page of results. */
@property(nonatomic, copy, nullable) NSString *nextPageToken;

/**
 *  The list of service configuration resources.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Service *> *serviceConfigs;

@end


/**
 *  Response message for ListServiceRollouts method.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "rollouts" property. If returned as the result of a query, it
 *        should support automatic pagination (when @c shouldFetchNextPages is
 *        enabled).
 */
@interface GTLRServiceManagement_ListServiceRolloutsResponse : GTLRCollectionObject

/** The token of the next page of results. */
@property(nonatomic, copy, nullable) NSString *nextPageToken;

/**
 *  The list of rollout resources.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Rollout *> *rollouts;

@end


/**
 *  Response message for `ListServices` method.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "services" property. If returned as the result of a query, it
 *        should support automatic pagination (when @c shouldFetchNextPages is
 *        enabled).
 */
@interface GTLRServiceManagement_ListServicesResponse : GTLRCollectionObject

/** Token that can be passed to `ListServices` to resume a paginated query. */
@property(nonatomic, copy, nullable) NSString *nextPageToken;

/**
 *  The returned services will only have the name field set.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_ManagedService *> *services;

@end


/**
 *  A description of a log type. Example in YAML format:
 *  - name: library.googleapis.com/activity_history
 *  description: The history of borrowing and returning library items.
 *  display_name: Activity
 *  labels:
 *  - key: /customer_id
 *  description: Identifier of a library customer
 */
@interface GTLRServiceManagement_LogDescriptor : GTLRObject

/**
 *  A human-readable description of this log. This information appears in
 *  the documentation and can contain details.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  The human-readable name for this log. This information appears on
 *  the user interface and should be concise.
 */
@property(nonatomic, copy, nullable) NSString *displayName;

/**
 *  The set of labels that are available to describe a specific log entry.
 *  Runtime requests that contain labels not specified here are
 *  considered invalid.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_LabelDescriptor *> *labels;

/**
 *  The name of the log. It must be less than 512 characters long and can
 *  include the following characters: upper- and lower-case alphanumeric
 *  characters [A-Za-z0-9], and punctuation characters including
 *  slash, underscore, hyphen, period [/_-.].
 */
@property(nonatomic, copy, nullable) NSString *name;

@end


/**
 *  Logging configuration of the service.
 *  The following example shows how to configure logs to be sent to the
 *  producer and consumer projects. In the example, the `activity_history`
 *  log is sent to both the producer and consumer projects, whereas the
 *  `purchase_history` log is only sent to the producer project.
 *  monitored_resources:
 *  - type: library.googleapis.com/branch
 *  labels:
 *  - key: /city
 *  description: The city where the library branch is located in.
 *  - key: /name
 *  description: The name of the branch.
 *  logs:
 *  - name: activity_history
 *  labels:
 *  - key: /customer_id
 *  - name: purchase_history
 *  logging:
 *  producer_destinations:
 *  - monitored_resource: library.googleapis.com/branch
 *  logs:
 *  - activity_history
 *  - purchase_history
 *  consumer_destinations:
 *  - monitored_resource: library.googleapis.com/branch
 *  logs:
 *  - activity_history
 */
@interface GTLRServiceManagement_Logging : GTLRObject

/**
 *  Logging configurations for sending logs to the consumer project.
 *  There can be multiple consumer destinations, each one must have a
 *  different monitored resource type. A log can be used in at most
 *  one consumer destination.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_LoggingDestination *> *consumerDestinations;

/**
 *  Logging configurations for sending logs to the producer project.
 *  There can be multiple producer destinations, each one must have a
 *  different monitored resource type. A log can be used in at most
 *  one producer destination.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_LoggingDestination *> *producerDestinations;

@end


/**
 *  Configuration of a specific logging destination (the producer project
 *  or the consumer project).
 */
@interface GTLRServiceManagement_LoggingDestination : GTLRObject

/**
 *  Names of the logs to be sent to this destination. Each name must
 *  be defined in the Service.logs section. If the log name is
 *  not a domain scoped name, it will be automatically prefixed with
 *  the service name followed by "/".
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *logs;

/**
 *  The monitored resource type. The type must be defined in the
 *  Service.monitored_resources section.
 */
@property(nonatomic, copy, nullable) NSString *monitoredResource;

@end


/**
 *  The full representation of a Service that is managed by
 *  Google Service Management.
 */
@interface GTLRServiceManagement_ManagedService : GTLRObject

/** ID of the project that produces and owns this service. */
@property(nonatomic, copy, nullable) NSString *producerProjectId;

/**
 *  The name of the service. See the [overview](/service-management/overview)
 *  for naming requirements.
 */
@property(nonatomic, copy, nullable) NSString *serviceName;

@end


/**
 *  Method represents a method of an API interface.
 */
@interface GTLRServiceManagement_Method : GTLRObject

/** The simple name of this method. */
@property(nonatomic, copy, nullable) NSString *name;

/** Any metadata attached to the method. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Option *> *options;

/**
 *  If true, the request is streamed.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *requestStreaming;

/** A URL of the input message type. */
@property(nonatomic, copy, nullable) NSString *requestTypeUrl;

/**
 *  If true, the response is streamed.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *responseStreaming;

/** The URL of the output message type. */
@property(nonatomic, copy, nullable) NSString *responseTypeUrl;

/**
 *  The source syntax of this method.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Method_Syntax_SyntaxProto2 Syntax `proto2`.
 *        (Value: "SYNTAX_PROTO2")
 *    @arg @c kGTLRServiceManagement_Method_Syntax_SyntaxProto3 Syntax `proto3`.
 *        (Value: "SYNTAX_PROTO3")
 */
@property(nonatomic, copy, nullable) NSString *syntax;

@end


/**
 *  Defines a metric type and its schema. Once a metric descriptor is created,
 *  deleting or altering it stops data collection and makes the metric type's
 *  existing data unusable.
 */
@interface GTLRServiceManagement_MetricDescriptor : GTLRObject

/**
 *  A detailed description of the metric, which can be used in documentation.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  A concise name for the metric, which can be displayed in user interfaces.
 *  Use sentence case without an ending period, for example "Request count".
 *  This field is optional but it is recommended to be set for any metrics
 *  associated with user-visible concepts, such as Quota.
 */
@property(nonatomic, copy, nullable) NSString *displayName;

/**
 *  The set of labels that can be used to describe a specific
 *  instance of this metric type. For example, the
 *  `appengine.googleapis.com/http/server/response_latencies` metric
 *  type has a label for the HTTP response code, `response_code`, so
 *  you can look at latencies for successful responses or just
 *  for responses that failed.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_LabelDescriptor *> *labels;

/**
 *  Optional. The launch stage of the metric definition.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_Alpha Alpha is
 *        a limited availability test for releases before they are cleared
 *        for widespread use. By Alpha, all significant design issues are
 *        resolved
 *        and we are in the process of verifying functionality. Alpha customers
 *        need to apply for access, agree to applicable terms, and have their
 *        projects whitelisted. Alpha releases don’t have to be feature
 *        complete,
 *        no SLAs are provided, and there are no technical support obligations,
 *        but
 *        they will be far enough along that customers can actually use them in
 *        test environments or for limited-use tests -- just like they would in
 *        normal production cases. (Value: "ALPHA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_Beta Beta is
 *        the point at which we are ready to open a release for any
 *        customer to use. There are no SLA or technical support obligations in
 *        a
 *        Beta release. Products will be complete from a feature perspective,
 *        but
 *        may have some open outstanding issues. Beta releases are suitable for
 *        limited production use cases. (Value: "BETA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_Deprecated
 *        Deprecated features are scheduled to be shut down and removed. For
 *        more
 *        information, see the “Deprecation Policy” section of our [Terms of
 *        Service](https://cloud.google.com/terms/)
 *        and the [Google Cloud Platform Subject to the Deprecation
 *        Policy](https://cloud.google.com/terms/deprecation) documentation.
 *        (Value: "DEPRECATED")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_EarlyAccess
 *        Early Access features are limited to a closed group of testers. To use
 *        these features, you must sign up in advance and sign a Trusted Tester
 *        agreement (which includes confidentiality provisions). These features
 *        may
 *        be unstable, changed in backward-incompatible ways, and are not
 *        guaranteed to be released. (Value: "EARLY_ACCESS")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_Ga GA features
 *        are open to all developers and are considered stable and
 *        fully qualified for production use. (Value: "GA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_LaunchStageUnspecified
 *        Do not use this default value. (Value: "LAUNCH_STAGE_UNSPECIFIED")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_Prelaunch
 *        Prelaunch features are hidden from users and are only visible
 *        internally. (Value: "PRELAUNCH")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_LaunchStage_Unimplemented
 *        The feature is not yet implemented. Users can not use it. (Value:
 *        "UNIMPLEMENTED")
 */
@property(nonatomic, copy, nullable) NSString *launchStage;

/** Optional. Metadata which can be used to guide usage of the metric. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_MetricDescriptorMetadata *metadata;

/**
 *  Whether the metric records instantaneous values, changes to a value, etc.
 *  Some combinations of `metric_kind` and `value_type` might not be supported.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_MetricKind_Cumulative A
 *        value accumulated over a time interval. Cumulative
 *        measurements in a time series should have the same start time
 *        and increasing end times, until an event resets the cumulative
 *        value to zero and sets a new start time for the following
 *        points. (Value: "CUMULATIVE")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_MetricKind_Delta The
 *        change in a value during a time interval. (Value: "DELTA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_MetricKind_Gauge An
 *        instantaneous measurement of a value. (Value: "GAUGE")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_MetricKind_MetricKindUnspecified
 *        Do not use this default value. (Value: "METRIC_KIND_UNSPECIFIED")
 */
@property(nonatomic, copy, nullable) NSString *metricKind;

/**
 *  Read-only. If present, then a time
 *  series, which is identified partially by
 *  a metric type and a MonitoredResourceDescriptor, that is associated
 *  with this metric type can only be associated with one of the monitored
 *  resource types listed here.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *monitoredResourceTypes;

/** The resource name of the metric descriptor. */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  The metric type, including its DNS name prefix. The type is not
 *  URL-encoded. All user-defined metric types have the DNS name
 *  `custom.googleapis.com` or `external.googleapis.com`. Metric types should
 *  use a natural hierarchical grouping. For example:
 *  "custom.googleapis.com/invoice/paid/amount"
 *  "external.googleapis.com/prometheus/up"
 *  "appengine.googleapis.com/http/server/response_latencies"
 */
@property(nonatomic, copy, nullable) NSString *type;

/**
 *  The units in which the metric value is reported. It is only applicable
 *  if the `value_type` is `INT64`, `DOUBLE`, or `DISTRIBUTION`. The `unit`
 *  defines the representation of the stored metric values.
 *  Different systems may scale the values to be more easily displayed (so a
 *  value of `0.02KBy` _might_ be displayed as `20By`, and a value of
 *  `3523KBy` _might_ be displayed as `3.5MBy`). However, if the `unit` is
 *  `KBy`, then the value of the metric is always in thousands of bytes, no
 *  matter how it may be displayed..
 *  If you want a custom metric to record the exact number of CPU-seconds used
 *  by a job, you can create an `INT64 CUMULATIVE` metric whose `unit` is
 *  `s{CPU}` (or equivalently `1s{CPU}` or just `s`). If the job uses 12,005
 *  CPU-seconds, then the value is written as `12005`.
 *  Alternatively, if you want a custom metric to record data in a more
 *  granular way, you can create a `DOUBLE CUMULATIVE` metric whose `unit` is
 *  `ks{CPU}`, and then write the value `12.005` (which is `12005/1000`),
 *  or use `Kis{CPU}` and write `11.723` (which is `12005/1024`).
 *  The supported units are a subset of [The Unified Code for Units of
 *  Measure](http://unitsofmeasure.org/ucum.html) standard:
 *  **Basic units (UNIT)**
 *  * `bit` bit
 *  * `By` byte
 *  * `s` second
 *  * `min` minute
 *  * `h` hour
 *  * `d` day
 *  **Prefixes (PREFIX)**
 *  * `k` kilo (10^3)
 *  * `M` mega (10^6)
 *  * `G` giga (10^9)
 *  * `T` tera (10^12)
 *  * `P` peta (10^15)
 *  * `E` exa (10^18)
 *  * `Z` zetta (10^21)
 *  * `Y` yotta (10^24)
 *  * `m` milli (10^-3)
 *  * `u` micro (10^-6)
 *  * `n` nano (10^-9)
 *  * `p` pico (10^-12)
 *  * `f` femto (10^-15)
 *  * `a` atto (10^-18)
 *  * `z` zepto (10^-21)
 *  * `y` yocto (10^-24)
 *  * `Ki` kibi (2^10)
 *  * `Mi` mebi (2^20)
 *  * `Gi` gibi (2^30)
 *  * `Ti` tebi (2^40)
 *  * `Pi` pebi (2^50)
 *  **Grammar**
 *  The grammar also includes these connectors:
 *  * `/` division or ratio (as an infix operator). For examples,
 *  `kBy/{email}` or `MiBy/10ms` (although you should almost never
 *  have `/s` in a metric `unit`; rates should always be computed at
 *  query time from the underlying cumulative or delta value).
 *  * `.` multiplication or composition (as an infix operator). For
 *  examples, `GBy.d` or `k{watt}.h`.
 *  The grammar for a unit is as follows:
 *  Expression = Component { "." Component } { "/" Component } ;
 *  Component = ( [ PREFIX ] UNIT | "%" ) [ Annotation ]
 *  | Annotation
 *  | "1"
 *  ;
 *  Annotation = "{" NAME "}" ;
 *  Notes:
 *  * `Annotation` is just a comment if it follows a `UNIT`. If the annotation
 *  is used alone, then the unit is equivalent to `1`. For examples,
 *  `{request}/s == 1/s`, `By{transmitted}/s == By/s`.
 *  * `NAME` is a sequence of non-blank printable ASCII characters not
 *  containing `{` or `}`.
 *  * `1` represents a unitary [dimensionless
 *  unit](https://en.wikipedia.org/wiki/Dimensionless_quantity) of 1, such
 *  as in `1/s`. It is typically used when none of the basic units are
 *  appropriate. For example, "new users per day" can be represented as
 *  `1/d` or `{new-users}/d` (and a metric value `5` would mean "5 new
 *  users). Alternatively, "thousands of page views per day" would be
 *  represented as `1000/d` or `k1/d` or `k{page_views}/d` (and a metric
 *  value of `5.3` would mean "5300 page views per day").
 *  * `%` represents dimensionless value of 1/100, and annotates values giving
 *  a percentage (so the metric values are typically in the range of 0..100,
 *  and a metric value `3` means "3 percent").
 *  * `10^2.%` indicates a metric contains a ratio, typically in the range
 *  0..1, that will be multiplied by 100 and displayed as a percentage
 *  (so a metric value `0.03` means "3 percent").
 */
@property(nonatomic, copy, nullable) NSString *unit;

/**
 *  Whether the measurement is an integer, a floating-point number, etc.
 *  Some combinations of `metric_kind` and `value_type` might not be supported.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_Bool The value
 *        is a boolean.
 *        This value type can be used only if the metric kind is `GAUGE`.
 *        (Value: "BOOL")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_Distribution The
 *        value is a `Distribution`. (Value: "DISTRIBUTION")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_Double The value
 *        is a double precision floating point number. (Value: "DOUBLE")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_Int64 The value
 *        is a signed 64-bit integer. (Value: "INT64")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_Money The value
 *        is money. (Value: "MONEY")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_String The value
 *        is a text string.
 *        This value type can be used only if the metric kind is `GAUGE`.
 *        (Value: "STRING")
 *    @arg @c kGTLRServiceManagement_MetricDescriptor_ValueType_ValueTypeUnspecified
 *        Do not use this default value. (Value: "VALUE_TYPE_UNSPECIFIED")
 */
@property(nonatomic, copy, nullable) NSString *valueType;

@end


/**
 *  Additional annotations that can be used to guide the usage of a metric.
 */
@interface GTLRServiceManagement_MetricDescriptorMetadata : GTLRObject

/**
 *  The delay of data points caused by ingestion. Data points older than this
 *  age are guaranteed to be ingested and available to be read, excluding
 *  data loss due to errors.
 */
@property(nonatomic, strong, nullable) GTLRDuration *ingestDelay;

/**
 *  Deprecated. Must use the MetricDescriptor.launch_stage instead.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Alpha
 *        Alpha is a limited availability test for releases before they are
 *        cleared
 *        for widespread use. By Alpha, all significant design issues are
 *        resolved
 *        and we are in the process of verifying functionality. Alpha customers
 *        need to apply for access, agree to applicable terms, and have their
 *        projects whitelisted. Alpha releases don’t have to be feature
 *        complete,
 *        no SLAs are provided, and there are no technical support obligations,
 *        but
 *        they will be far enough along that customers can actually use them in
 *        test environments or for limited-use tests -- just like they would in
 *        normal production cases. (Value: "ALPHA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Beta
 *        Beta is the point at which we are ready to open a release for any
 *        customer to use. There are no SLA or technical support obligations in
 *        a
 *        Beta release. Products will be complete from a feature perspective,
 *        but
 *        may have some open outstanding issues. Beta releases are suitable for
 *        limited production use cases. (Value: "BETA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Deprecated
 *        Deprecated features are scheduled to be shut down and removed. For
 *        more
 *        information, see the “Deprecation Policy” section of our [Terms of
 *        Service](https://cloud.google.com/terms/)
 *        and the [Google Cloud Platform Subject to the Deprecation
 *        Policy](https://cloud.google.com/terms/deprecation) documentation.
 *        (Value: "DEPRECATED")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_EarlyAccess
 *        Early Access features are limited to a closed group of testers. To use
 *        these features, you must sign up in advance and sign a Trusted Tester
 *        agreement (which includes confidentiality provisions). These features
 *        may
 *        be unstable, changed in backward-incompatible ways, and are not
 *        guaranteed to be released. (Value: "EARLY_ACCESS")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Ga GA
 *        features are open to all developers and are considered stable and
 *        fully qualified for production use. (Value: "GA")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_LaunchStageUnspecified
 *        Do not use this default value. (Value: "LAUNCH_STAGE_UNSPECIFIED")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Prelaunch
 *        Prelaunch features are hidden from users and are only visible
 *        internally. (Value: "PRELAUNCH")
 *    @arg @c kGTLRServiceManagement_MetricDescriptorMetadata_LaunchStage_Unimplemented
 *        The feature is not yet implemented. Users can not use it. (Value:
 *        "UNIMPLEMENTED")
 */
@property(nonatomic, copy, nullable) NSString *launchStage;

/**
 *  The sampling period of metric data points. For metrics which are written
 *  periodically, consecutive data points are stored at this time interval,
 *  excluding data loss due to errors. Metrics with a higher granularity have
 *  a smaller sampling period.
 */
@property(nonatomic, strong, nullable) GTLRDuration *samplePeriod;

@end


/**
 *  Bind API methods to metrics. Binding a method to a metric causes that
 *  metric's configured quota behaviors to apply to the method call.
 */
@interface GTLRServiceManagement_MetricRule : GTLRObject

/**
 *  Metrics to update when the selected methods are called, and the associated
 *  cost applied to each metric.
 *  The key of the map is the metric name, and the values are the amount
 *  increased for the metric against which the quota limits are defined.
 *  The value must not be negative.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_MetricRule_MetricCosts *metricCosts;

/**
 *  Selects the methods to which this rule applies.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  Metrics to update when the selected methods are called, and the associated
 *  cost applied to each metric.
 *  The key of the map is the metric name, and the values are the amount
 *  increased for the metric against which the quota limits are defined.
 *  The value must not be negative.
 *
 *  @note This class is documented as having more properties of NSNumber (Uses
 *        NSNumber of longLongValue.). Use @c -additionalJSONKeys and @c
 *        -additionalPropertyForName: to get the list of properties and then
 *        fetch them; or @c -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_MetricRule_MetricCosts : GTLRObject
@end


/**
 *  Declares an API Interface to be included in this interface. The including
 *  interface must redeclare all the methods from the included interface, but
 *  documentation and options are inherited as follows:
 *  - If after comment and whitespace stripping, the documentation
 *  string of the redeclared method is empty, it will be inherited
 *  from the original method.
 *  - Each annotation belonging to the service config (http,
 *  visibility) which is not set in the redeclared method will be
 *  inherited.
 *  - If an http annotation is inherited, the path pattern will be
 *  modified as follows. Any version prefix will be replaced by the
 *  version of the including interface plus the root path if
 *  specified.
 *  Example of a simple mixin:
 *  package google.acl.v1;
 *  service AccessControl {
 *  // Get the underlying ACL object.
 *  rpc GetAcl(GetAclRequest) returns (Acl) {
 *  option (google.api.http).get = "/v1/{resource=**}:getAcl";
 *  }
 *  }
 *  package google.storage.v2;
 *  service Storage {
 *  // rpc GetAcl(GetAclRequest) returns (Acl);
 *  // Get a data record.
 *  rpc GetData(GetDataRequest) returns (Data) {
 *  option (google.api.http).get = "/v2/{resource=**}";
 *  }
 *  }
 *  Example of a mixin configuration:
 *  apis:
 *  - name: google.storage.v2.Storage
 *  mixins:
 *  - name: google.acl.v1.AccessControl
 *  The mixin construct implies that all methods in `AccessControl` are
 *  also declared with same name and request/response types in
 *  `Storage`. A documentation generator or annotation processor will
 *  see the effective `Storage.GetAcl` method after inherting
 *  documentation and annotations as follows:
 *  service Storage {
 *  // Get the underlying ACL object.
 *  rpc GetAcl(GetAclRequest) returns (Acl) {
 *  option (google.api.http).get = "/v2/{resource=**}:getAcl";
 *  }
 *  ...
 *  }
 *  Note how the version in the path pattern changed from `v1` to `v2`.
 *  If the `root` field in the mixin is specified, it should be a
 *  relative path under which inherited HTTP paths are placed. Example:
 *  apis:
 *  - name: google.storage.v2.Storage
 *  mixins:
 *  - name: google.acl.v1.AccessControl
 *  root: acls
 *  This implies the following inherited HTTP annotation:
 *  service Storage {
 *  // Get the underlying ACL object.
 *  rpc GetAcl(GetAclRequest) returns (Acl) {
 *  option (google.api.http).get = "/v2/acls/{resource=**}:getAcl";
 *  }
 *  ...
 *  }
 */
@interface GTLRServiceManagement_Mixin : GTLRObject

/** The fully qualified name of the interface which is included. */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  If non-empty specifies a path under which inherited HTTP paths
 *  are rooted.
 */
@property(nonatomic, copy, nullable) NSString *root;

@end


/**
 *  An object that describes the schema of a MonitoredResource object using a
 *  type name and a set of labels. For example, the monitored resource
 *  descriptor for Google Compute Engine VM instances has a type of
 *  `"gce_instance"` and specifies the use of the labels `"instance_id"` and
 *  `"zone"` to identify particular VM instances.
 *  Different APIs can support different monitored resource types. APIs
 *  generally
 *  provide a `list` method that returns the monitored resource descriptors used
 *  by the API.
 */
@interface GTLRServiceManagement_MonitoredResourceDescriptor : GTLRObject

/**
 *  Optional. A detailed description of the monitored resource type that might
 *  be used in documentation.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  Optional. A concise name for the monitored resource type that might be
 *  displayed in user interfaces. It should be a Title Cased Noun Phrase,
 *  without any article or other determiners. For example,
 *  `"Google Cloud SQL Database"`.
 */
@property(nonatomic, copy, nullable) NSString *displayName;

/**
 *  Required. A set of labels used to describe instances of this monitored
 *  resource type. For example, an individual Google Cloud SQL database is
 *  identified by values for the labels `"database_id"` and `"zone"`.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_LabelDescriptor *> *labels;

/**
 *  Optional. The launch stage of the monitored resource definition.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Alpha
 *        Alpha is a limited availability test for releases before they are
 *        cleared
 *        for widespread use. By Alpha, all significant design issues are
 *        resolved
 *        and we are in the process of verifying functionality. Alpha customers
 *        need to apply for access, agree to applicable terms, and have their
 *        projects whitelisted. Alpha releases don’t have to be feature
 *        complete,
 *        no SLAs are provided, and there are no technical support obligations,
 *        but
 *        they will be far enough along that customers can actually use them in
 *        test environments or for limited-use tests -- just like they would in
 *        normal production cases. (Value: "ALPHA")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Beta
 *        Beta is the point at which we are ready to open a release for any
 *        customer to use. There are no SLA or technical support obligations in
 *        a
 *        Beta release. Products will be complete from a feature perspective,
 *        but
 *        may have some open outstanding issues. Beta releases are suitable for
 *        limited production use cases. (Value: "BETA")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Deprecated
 *        Deprecated features are scheduled to be shut down and removed. For
 *        more
 *        information, see the “Deprecation Policy” section of our [Terms of
 *        Service](https://cloud.google.com/terms/)
 *        and the [Google Cloud Platform Subject to the Deprecation
 *        Policy](https://cloud.google.com/terms/deprecation) documentation.
 *        (Value: "DEPRECATED")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_EarlyAccess
 *        Early Access features are limited to a closed group of testers. To use
 *        these features, you must sign up in advance and sign a Trusted Tester
 *        agreement (which includes confidentiality provisions). These features
 *        may
 *        be unstable, changed in backward-incompatible ways, and are not
 *        guaranteed to be released. (Value: "EARLY_ACCESS")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Ga
 *        GA features are open to all developers and are considered stable and
 *        fully qualified for production use. (Value: "GA")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_LaunchStageUnspecified
 *        Do not use this default value. (Value: "LAUNCH_STAGE_UNSPECIFIED")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Prelaunch
 *        Prelaunch features are hidden from users and are only visible
 *        internally. (Value: "PRELAUNCH")
 *    @arg @c kGTLRServiceManagement_MonitoredResourceDescriptor_LaunchStage_Unimplemented
 *        The feature is not yet implemented. Users can not use it. (Value:
 *        "UNIMPLEMENTED")
 */
@property(nonatomic, copy, nullable) NSString *launchStage;

/**
 *  Optional. The resource name of the monitored resource descriptor:
 *  `"projects/{project_id}/monitoredResourceDescriptors/{type}"` where
 *  {type} is the value of the `type` field in this object and
 *  {project_id} is a project ID that provides API-specific context for
 *  accessing the type. APIs that do not use project information can use the
 *  resource name format `"monitoredResourceDescriptors/{type}"`.
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  Required. The monitored resource type. For example, the type
 *  `"cloudsql_database"` represents databases in Google Cloud SQL.
 *  The maximum length of this value is 256 characters.
 */
@property(nonatomic, copy, nullable) NSString *type;

@end


/**
 *  Monitoring configuration of the service.
 *  The example below shows how to configure monitored resources and metrics
 *  for monitoring. In the example, a monitored resource and two metrics are
 *  defined. The `library.googleapis.com/book/returned_count` metric is sent
 *  to both producer and consumer projects, whereas the
 *  `library.googleapis.com/book/overdue_count` metric is only sent to the
 *  consumer project.
 *  monitored_resources:
 *  - type: library.googleapis.com/branch
 *  labels:
 *  - key: /city
 *  description: The city where the library branch is located in.
 *  - key: /name
 *  description: The name of the branch.
 *  metrics:
 *  - name: library.googleapis.com/book/returned_count
 *  metric_kind: DELTA
 *  value_type: INT64
 *  labels:
 *  - key: /customer_id
 *  - name: library.googleapis.com/book/overdue_count
 *  metric_kind: GAUGE
 *  value_type: INT64
 *  labels:
 *  - key: /customer_id
 *  monitoring:
 *  producer_destinations:
 *  - monitored_resource: library.googleapis.com/branch
 *  metrics:
 *  - library.googleapis.com/book/returned_count
 *  consumer_destinations:
 *  - monitored_resource: library.googleapis.com/branch
 *  metrics:
 *  - library.googleapis.com/book/returned_count
 *  - library.googleapis.com/book/overdue_count
 */
@interface GTLRServiceManagement_Monitoring : GTLRObject

/**
 *  Monitoring configurations for sending metrics to the consumer project.
 *  There can be multiple consumer destinations. A monitored resouce type may
 *  appear in multiple monitoring destinations if different aggregations are
 *  needed for different sets of metrics associated with that monitored
 *  resource type. A monitored resource and metric pair may only be used once
 *  in the Monitoring configuration.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_MonitoringDestination *> *consumerDestinations;

/**
 *  Monitoring configurations for sending metrics to the producer project.
 *  There can be multiple producer destinations. A monitored resouce type may
 *  appear in multiple monitoring destinations if different aggregations are
 *  needed for different sets of metrics associated with that monitored
 *  resource type. A monitored resource and metric pair may only be used once
 *  in the Monitoring configuration.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_MonitoringDestination *> *producerDestinations;

@end


/**
 *  Configuration of a specific monitoring destination (the producer project
 *  or the consumer project).
 */
@interface GTLRServiceManagement_MonitoringDestination : GTLRObject

/**
 *  Types of the metrics to report to this monitoring destination.
 *  Each type must be defined in Service.metrics section.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *metrics;

/**
 *  The monitored resource type. The type must be defined in
 *  Service.monitored_resources section.
 */
@property(nonatomic, copy, nullable) NSString *monitoredResource;

@end


/**
 *  OAuth scopes are a way to define data and permissions on data. For example,
 *  there are scopes defined for "Read-only access to Google Calendar" and
 *  "Access to Cloud Platform". Users can consent to a scope for an application,
 *  giving it permission to access that data on their behalf.
 *  OAuth scope specifications should be fairly coarse grained; a user will need
 *  to see and understand the text description of what your scope means.
 *  In most cases: use one or at most two OAuth scopes for an entire family of
 *  products. If your product has multiple APIs, you should probably be sharing
 *  the OAuth scope across all of those APIs.
 *  When you need finer grained OAuth consent screens: talk with your product
 *  management about how developers will use them in practice.
 *  Please note that even though each of the canonical scopes is enough for a
 *  request to be accepted and passed to the backend, a request can still fail
 *  due to the backend requiring additional scopes or permissions.
 */
@interface GTLRServiceManagement_OAuthRequirements : GTLRObject

/**
 *  The list of publicly documented OAuth scopes that are allowed access. An
 *  OAuth token containing any of these scopes will be accepted.
 *  Example:
 *  canonical_scopes: https://www.googleapis.com/auth/calendar,
 *  https://www.googleapis.com/auth/calendar.read
 */
@property(nonatomic, copy, nullable) NSString *canonicalScopes;

@end


/**
 *  This resource represents a long-running operation that is the result of a
 *  network API call.
 */
@interface GTLRServiceManagement_Operation : GTLRObject

/**
 *  If the value is `false`, it means the operation is still in progress.
 *  If `true`, the operation is completed, and either `error` or `response` is
 *  available.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *done;

/** The error result of the operation in case of failure or cancellation. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Status *error;

/**
 *  Service-specific metadata associated with the operation. It typically
 *  contains progress information and common metadata such as create time.
 *  Some services might not provide such metadata. Any method that returns a
 *  long-running operation should document the metadata type, if any.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Operation_Metadata *metadata;

/**
 *  The server-assigned name, which is only unique within the same service that
 *  originally returns it. If you use the default HTTP mapping, the
 *  `name` should be a resource name ending with `operations/{unique_id}`.
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  The normal response of the operation in case of success. If the original
 *  method returns no data on success, such as `Delete`, the response is
 *  `google.protobuf.Empty`. If the original method is standard
 *  `Get`/`Create`/`Update`, the response should be the resource. For other
 *  methods, the response should have the type `XxxResponse`, where `Xxx`
 *  is the original method name. For example, if the original method name
 *  is `TakeSnapshot()`, the inferred response type is
 *  `TakeSnapshotResponse`.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Operation_Response *response;

@end


/**
 *  Service-specific metadata associated with the operation. It typically
 *  contains progress information and common metadata such as create time.
 *  Some services might not provide such metadata. Any method that returns a
 *  long-running operation should document the metadata type, if any.
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_Operation_Metadata : GTLRObject
@end


/**
 *  The normal response of the operation in case of success. If the original
 *  method returns no data on success, such as `Delete`, the response is
 *  `google.protobuf.Empty`. If the original method is standard
 *  `Get`/`Create`/`Update`, the response should be the resource. For other
 *  methods, the response should have the type `XxxResponse`, where `Xxx`
 *  is the original method name. For example, if the original method name
 *  is `TakeSnapshot()`, the inferred response type is
 *  `TakeSnapshotResponse`.
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_Operation_Response : GTLRObject
@end


/**
 *  The metadata associated with a long running operation resource.
 */
@interface GTLRServiceManagement_OperationMetadata : GTLRObject

/**
 *  Percentage of completion of this operation, ranging from 0 to 100.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *progressPercentage;

/**
 *  The full name of the resources that this operation is directly
 *  associated with.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *resourceNames;

/** The start time of the operation. */
@property(nonatomic, strong, nullable) GTLRDateTime *startTime;

/** Detailed status information for each step. The order is undetermined. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Step *> *steps;

@end


/**
 *  A protocol buffer option, which can be attached to a message, field,
 *  enumeration, etc.
 */
@interface GTLRServiceManagement_Option : GTLRObject

/**
 *  The option's name. For protobuf built-in options (options defined in
 *  descriptor.proto), this is the short name. For example, `"map_entry"`.
 *  For custom options, it should be the fully-qualified name. For example,
 *  `"google.api.http"`.
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  The option's value packed in an Any message. If the value is a primitive,
 *  the corresponding wrapper type defined in google/protobuf/wrappers.proto
 *  should be used. If the value is an enum, it should be stored as an int32
 *  value using the google.protobuf.Int32Value type.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Option_Value *value;

@end


/**
 *  The option's value packed in an Any message. If the value is a primitive,
 *  the corresponding wrapper type defined in google/protobuf/wrappers.proto
 *  should be used. If the value is an enum, it should be stored as an int32
 *  value using the google.protobuf.Int32Value type.
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_Option_Value : GTLRObject
@end


/**
 *  Represents a documentation page. A page can contain subpages to represent
 *  nested documentation set structure.
 */
@interface GTLRServiceManagement_Page : GTLRObject

/**
 *  The Markdown content of the page. You can use <code>&#40;== include {path}
 *  ==&#41;</code> to include content from a Markdown file.
 */
@property(nonatomic, copy, nullable) NSString *content;

/**
 *  The name of the page. It will be used as an identity of the page to
 *  generate URI of the page, text of the link to this page in navigation,
 *  etc. The full page name (start from the root page name to this page
 *  concatenated with `.`) can be used as reference to the page in your
 *  documentation. For example:
 *  <pre><code>pages:
 *  - name: Tutorial
 *  content: &#40;== include tutorial.md ==&#41;
 *  subpages:
 *  - name: Java
 *  content: &#40;== include tutorial_java.md ==&#41;
 *  </code></pre>
 *  You can reference `Java` page using Markdown reference link syntax:
 *  `Java`.
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  Subpages of this page. The order of subpages specified here will be
 *  honored in the generated docset.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Page *> *subpages;

@end


/**
 *  An Identity and Access Management (IAM) policy, which specifies access
 *  controls for Google Cloud resources.
 *  A `Policy` is a collection of `bindings`. A `binding` binds one or more
 *  `members` to a single `role`. Members can be user accounts, service
 *  accounts,
 *  Google groups, and domains (such as G Suite). A `role` is a named list of
 *  permissions; each `role` can be an IAM predefined role or a user-created
 *  custom role.
 *  Optionally, a `binding` can specify a `condition`, which is a logical
 *  expression that allows access to a resource only if the expression evaluates
 *  to `true`. A condition can add constraints based on attributes of the
 *  request, the resource, or both.
 *  **JSON example:**
 *  {
 *  "bindings": [
 *  {
 *  "role": "roles/resourcemanager.organizationAdmin",
 *  "members": [
 *  "user:mike\@example.com",
 *  "group:admins\@example.com",
 *  "domain:google.com",
 *  "serviceAccount:my-project-id\@appspot.gserviceaccount.com"
 *  ]
 *  },
 *  {
 *  "role": "roles/resourcemanager.organizationViewer",
 *  "members": ["user:eve\@example.com"],
 *  "condition": {
 *  "title": "expirable access",
 *  "description": "Does not grant access after Sep 2020",
 *  "expression": "request.time < timestamp('2020-10-01T00:00:00.000Z')",
 *  }
 *  }
 *  ],
 *  "etag": "BwWWja0YfJA=",
 *  "version": 3
 *  }
 *  **YAML example:**
 *  bindings:
 *  - members:
 *  - user:mike\@example.com
 *  - group:admins\@example.com
 *  - domain:google.com
 *  - serviceAccount:my-project-id\@appspot.gserviceaccount.com
 *  role: roles/resourcemanager.organizationAdmin
 *  - members:
 *  - user:eve\@example.com
 *  role: roles/resourcemanager.organizationViewer
 *  condition:
 *  title: expirable access
 *  description: Does not grant access after Sep 2020
 *  expression: request.time < timestamp('2020-10-01T00:00:00.000Z')
 *  - etag: BwWWja0YfJA=
 *  - version: 3
 *  For a description of IAM and its features, see the
 *  [IAM documentation](https://cloud.google.com/iam/docs/).
 */
@interface GTLRServiceManagement_Policy : GTLRObject

/** Specifies cloud audit logging configuration for this policy. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_AuditConfig *> *auditConfigs;

/**
 *  Associates a list of `members` to a `role`. Optionally, may specify a
 *  `condition` that determines how and when the `bindings` are applied. Each
 *  of the `bindings` must contain at least one member.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Binding *> *bindings;

/**
 *  `etag` is used for optimistic concurrency control as a way to help
 *  prevent simultaneous updates of a policy from overwriting each other.
 *  It is strongly suggested that systems make use of the `etag` in the
 *  read-modify-write cycle to perform policy updates in order to avoid race
 *  conditions: An `etag` is returned in the response to `getIamPolicy`, and
 *  systems are expected to put that etag in the request to `setIamPolicy` to
 *  ensure that their change will be applied to the same version of the policy.
 *  **Important:** If you use IAM Conditions, you must include the `etag` field
 *  whenever you call `setIamPolicy`. If you omit this field, then IAM allows
 *  you to overwrite a version `3` policy with a version `1` policy, and all of
 *  the conditions in the version `3` policy are lost.
 *
 *  Contains encoded binary data; GTLRBase64 can encode/decode (probably
 *  web-safe format).
 */
@property(nonatomic, copy, nullable) NSString *ETag;

/**
 *  Specifies the format of the policy.
 *  Valid values are `0`, `1`, and `3`. Requests that specify an invalid value
 *  are rejected.
 *  Any operation that affects conditional role bindings must specify version
 *  `3`. This requirement applies to the following operations:
 *  * Getting a policy that includes a conditional role binding
 *  * Adding a conditional role binding to a policy
 *  * Changing a conditional role binding in a policy
 *  * Removing any role binding, with or without a condition, from a policy
 *  that includes conditions
 *  **Important:** If you use IAM Conditions, you must include the `etag` field
 *  whenever you call `setIamPolicy`. If you omit this field, then IAM allows
 *  you to overwrite a version `3` policy with a version `1` policy, and all of
 *  the conditions in the version `3` policy are lost.
 *  If a policy does not include any conditions, operations on that policy may
 *  specify any valid version or leave the field unset.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *version;

@end


/**
 *  Quota configuration helps to achieve fairness and budgeting in service
 *  usage.
 *  The metric based quota configuration works this way:
 *  - The service configuration defines a set of metrics.
 *  - For API calls, the quota.metric_rules maps methods to metrics with
 *  corresponding costs.
 *  - The quota.limits defines limits on the metrics, which will be used for
 *  quota checks at runtime.
 *  An example quota configuration in yaml format:
 *  quota:
 *  limits:
 *  - name: apiWriteQpsPerProject
 *  metric: library.googleapis.com/write_calls
 *  unit: "1/min/{project}" # rate limit for consumer projects
 *  values:
 *  STANDARD: 10000
 *  # The metric rules bind all methods to the read_calls metric,
 *  # except for the UpdateBook and DeleteBook methods. These two methods
 *  # are mapped to the write_calls metric, with the UpdateBook method
 *  # consuming at twice rate as the DeleteBook method.
 *  metric_rules:
 *  - selector: "*"
 *  metric_costs:
 *  library.googleapis.com/read_calls: 1
 *  - selector: google.example.library.v1.LibraryService.UpdateBook
 *  metric_costs:
 *  library.googleapis.com/write_calls: 2
 *  - selector: google.example.library.v1.LibraryService.DeleteBook
 *  metric_costs:
 *  library.googleapis.com/write_calls: 1
 *  Corresponding Metric definition:
 *  metrics:
 *  - name: library.googleapis.com/read_calls
 *  display_name: Read requests
 *  metric_kind: DELTA
 *  value_type: INT64
 *  - name: library.googleapis.com/write_calls
 *  display_name: Write requests
 *  metric_kind: DELTA
 *  value_type: INT64
 */
@interface GTLRServiceManagement_Quota : GTLRObject

/** List of `QuotaLimit` definitions for the service. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_QuotaLimit *> *limits;

/**
 *  List of `MetricRule` definitions, each one mapping a selected method to one
 *  or more metrics.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_MetricRule *> *metricRules;

@end


/**
 *  `QuotaLimit` defines a specific limit that applies over a specified duration
 *  for a limit type. There can be at most one limit for a duration and limit
 *  type combination defined within a `QuotaGroup`.
 */
@interface GTLRServiceManagement_QuotaLimit : GTLRObject

/**
 *  Default number of tokens that can be consumed during the specified
 *  duration. This is the number of tokens assigned when a client
 *  application developer activates the service for his/her project.
 *  Specifying a value of 0 will block all requests. This can be used if you
 *  are provisioning quota to selected consumers and blocking others.
 *  Similarly, a value of -1 will indicate an unlimited quota. No other
 *  negative values are allowed.
 *  Used by group-based quotas only.
 *
 *  Uses NSNumber of longLongValue.
 */
@property(nonatomic, strong, nullable) NSNumber *defaultLimit;

/**
 *  Optional. User-visible, extended description for this quota limit.
 *  Should be used only when more context is needed to understand this limit
 *  than provided by the limit's display name (see: `display_name`).
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  User-visible display name for this limit.
 *  Optional. If not set, the UI will provide a default display name based on
 *  the quota configuration. This field can be used to override the default
 *  display name generated from the configuration.
 */
@property(nonatomic, copy, nullable) NSString *displayName;

/**
 *  Duration of this limit in textual notation. Must be "100s" or "1d".
 *  Used by group-based quotas only.
 */
@property(nonatomic, copy, nullable) NSString *duration;

/**
 *  Free tier value displayed in the Developers Console for this limit.
 *  The free tier is the number of tokens that will be subtracted from the
 *  billed amount when billing is enabled.
 *  This field can only be set on a limit with duration "1d", in a billable
 *  group; it is invalid on any other limit. If this field is not set, it
 *  defaults to 0, indicating that there is no free tier for this service.
 *  Used by group-based quotas only.
 *
 *  Uses NSNumber of longLongValue.
 */
@property(nonatomic, strong, nullable) NSNumber *freeTier;

/**
 *  Maximum number of tokens that can be consumed during the specified
 *  duration. Client application developers can override the default limit up
 *  to this maximum. If specified, this value cannot be set to a value less
 *  than the default limit. If not specified, it is set to the default limit.
 *  To allow clients to apply overrides with no upper bound, set this to -1,
 *  indicating unlimited maximum quota.
 *  Used by group-based quotas only.
 *
 *  Uses NSNumber of longLongValue.
 */
@property(nonatomic, strong, nullable) NSNumber *maxLimit;

/**
 *  The name of the metric this quota limit applies to. The quota limits with
 *  the same metric will be checked together during runtime. The metric must be
 *  defined within the service config.
 */
@property(nonatomic, copy, nullable) NSString *metric;

/**
 *  Name of the quota limit.
 *  The name must be provided, and it must be unique within the service. The
 *  name can only include alphanumeric characters as well as '-'.
 *  The maximum length of the limit name is 64 characters.
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  Specify the unit of the quota limit. It uses the same syntax as
 *  Metric.unit. The supported unit kinds are determined by the quota
 *  backend system.
 *  Here are some examples:
 *  * "1/min/{project}" for quota per minute per project.
 *  Note: the order of unit components is insignificant.
 *  The "1" at the beginning is required to follow the metric unit syntax.
 */
@property(nonatomic, copy, nullable) NSString *unit;

/**
 *  Tiered limit values. You must specify this as a key:value pair, with an
 *  integer value that is the maximum number of requests allowed for the
 *  specified unit. Currently only STANDARD is supported.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_QuotaLimit_Values *values;

@end


/**
 *  Tiered limit values. You must specify this as a key:value pair, with an
 *  integer value that is the maximum number of requests allowed for the
 *  specified unit. Currently only STANDARD is supported.
 *
 *  @note This class is documented as having more properties of NSNumber (Uses
 *        NSNumber of longLongValue.). Use @c -additionalJSONKeys and @c
 *        -additionalPropertyForName: to get the list of properties and then
 *        fetch them; or @c -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_QuotaLimit_Values : GTLRObject
@end


/**
 *  A rollout resource that defines how service configuration versions are
 *  pushed
 *  to control plane systems. Typically, you create a new version of the
 *  service config, and then create a Rollout to push the service config.
 */
@interface GTLRServiceManagement_Rollout : GTLRObject

/**
 *  This field is deprecated and will be deleted. Please remove usage of
 *  this field.
 */
@property(nonatomic, copy, nullable) NSString *createdBy;

/** Creation time of the rollout. Readonly. */
@property(nonatomic, strong, nullable) GTLRDateTime *createTime;

/**
 *  The strategy associated with a rollout to delete a `ManagedService`.
 *  Readonly.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_DeleteServiceStrategy *deleteServiceStrategy;

/**
 *  Optional. Unique identifier of this Rollout. Must be no longer than 63
 *  characters
 *  and only lower case letters, digits, '.', '_' and '-' are allowed.
 *  If not specified by client, the server will generate one. The generated id
 *  will have the form of <date><revision number>, where "date" is the create
 *  date in ISO 8601 format. "revision number" is a monotonically increasing
 *  positive number that is reset every day for each service.
 *  An example of the generated rollout_id is '2016-02-16r1'
 */
@property(nonatomic, copy, nullable) NSString *rolloutId;

/** The name of the service associated with this Rollout. */
@property(nonatomic, copy, nullable) NSString *serviceName;

/**
 *  The status of this rollout. Readonly. In case of a failed rollout,
 *  the system will automatically rollback to the current Rollout
 *  version. Readonly.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Rollout_Status_Cancelled The Rollout has
 *        been cancelled. This can happen if you have overlapping
 *        Rollout pushes, and the previous ones will be cancelled. (Value:
 *        "CANCELLED")
 *    @arg @c kGTLRServiceManagement_Rollout_Status_Failed The Rollout has
 *        failed and the rollback attempt has failed too. (Value: "FAILED")
 *    @arg @c kGTLRServiceManagement_Rollout_Status_FailedRolledBack The Rollout
 *        has failed and rolled back to the previous successful
 *        Rollout. (Value: "FAILED_ROLLED_BACK")
 *    @arg @c kGTLRServiceManagement_Rollout_Status_InProgress The Rollout is in
 *        progress. (Value: "IN_PROGRESS")
 *    @arg @c kGTLRServiceManagement_Rollout_Status_Pending The Rollout has not
 *        started yet and is pending for execution. (Value: "PENDING")
 *    @arg @c kGTLRServiceManagement_Rollout_Status_RolloutStatusUnspecified No
 *        status specified. (Value: "ROLLOUT_STATUS_UNSPECIFIED")
 *    @arg @c kGTLRServiceManagement_Rollout_Status_Success The Rollout has
 *        completed successfully. (Value: "SUCCESS")
 */
@property(nonatomic, copy, nullable) NSString *status;

/**
 *  Google Service Control selects service configurations based on
 *  traffic percentage.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_TrafficPercentStrategy *trafficPercentStrategy;

@end


/**
 *  `Service` is the root object of Google service configuration schema. It
 *  describes basic information about a service, such as the name and the
 *  title, and delegates other aspects to sub-sections. Each sub-section is
 *  either a proto message or a repeated proto message that configures a
 *  specific aspect, such as auth. See each proto message definition for
 *  details.
 *  Example:
 *  type: google.api.Service
 *  config_version: 3
 *  name: calendar.googleapis.com
 *  title: Google Calendar API
 *  apis:
 *  - name: google.calendar.v3.Calendar
 *  authentication:
 *  providers:
 *  - id: google_calendar_auth
 *  jwks_uri: https://www.googleapis.com/oauth2/v1/certs
 *  issuer: https://securetoken.google.com
 *  rules:
 *  - selector: "*"
 *  requirements:
 *  provider_id: google_calendar_auth
 */
@interface GTLRServiceManagement_Service : GTLRObject

/**
 *  A list of API interfaces exported by this service. Only the `name` field
 *  of the google.protobuf.Api needs to be provided by the configuration
 *  author, as the remaining fields will be derived from the IDL during the
 *  normalization process. It is an error to specify an API interface here
 *  which cannot be resolved against the associated IDL files.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Api *> *apis;

/** Auth configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Authentication *authentication;

/** API backend configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Backend *backend;

/** Billing configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Billing *billing;

/**
 *  The semantic version of the service configuration. The config version
 *  affects the interpretation of the service configuration. For example,
 *  certain features are enabled by default for certain config versions.
 *  The latest config version is `3`.
 *
 *  Uses NSNumber of unsignedIntValue.
 */
@property(nonatomic, strong, nullable) NSNumber *configVersion;

/** Context configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Context *context;

/** Configuration for the service control plane. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Control *control;

/** Custom error configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_CustomError *customError;

/** Additional API documentation. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Documentation *documentation;

/**
 *  Configuration for network endpoints. If this is empty, then an endpoint
 *  with the same name as the service is automatically generated to service all
 *  defined APIs.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Endpoint *> *endpoints;

/**
 *  A list of all enum types included in this API service. Enums
 *  referenced directly or indirectly by the `apis` are automatically
 *  included. Enums which are not referenced but shall be included
 *  should be listed here by name. Example:
 *  enums:
 *  - name: google.someapi.v1.SomeEnum
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Enum *> *enums;

/** HTTP configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Http *http;

/**
 *  A unique ID for a specific instance of this message, typically assigned
 *  by the client for tracking purpose. Must be no longer than 63 characters
 *  and only lower case letters, digits, '.', '_' and '-' are allowed. If
 *  empty, the server may choose to generate one instead.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 */
@property(nonatomic, copy, nullable) NSString *identifier;

/** Logging configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Logging *logging;

/** Defines the logs used by this service. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_LogDescriptor *> *logs;

/** Defines the metrics used by this service. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_MetricDescriptor *> *metrics;

/**
 *  Defines the monitored resources used by this service. This is required
 *  by the Service.monitoring and Service.logging configurations.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_MonitoredResourceDescriptor *> *monitoredResources;

/** Monitoring configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Monitoring *monitoring;

/**
 *  The service name, which is a DNS-like logical identifier for the
 *  service, such as `calendar.googleapis.com`. The service name
 *  typically goes through DNS verification to make sure the owner
 *  of the service also owns the DNS name.
 */
@property(nonatomic, copy, nullable) NSString *name;

/** The Google project that owns this service. */
@property(nonatomic, copy, nullable) NSString *producerProjectId;

/** Quota configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Quota *quota;

/**
 *  Output only. The source information for this configuration if available.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_SourceInfo *sourceInfo;

/** System parameter configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_SystemParameters *systemParameters;

/**
 *  A list of all proto message types included in this API service.
 *  It serves similar purpose as [google.api.Service.types], except that
 *  these types are not needed by user-defined APIs. Therefore, they will not
 *  show up in the generated discovery doc. This field should only be used
 *  to define system APIs in ESF.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Type *> *systemTypes;

/** The product title for this service. */
@property(nonatomic, copy, nullable) NSString *title;

/**
 *  A list of all proto message types included in this API service.
 *  Types referenced directly or indirectly by the `apis` are
 *  automatically included. Messages which are not referenced but
 *  shall be included, such as types used by the `google.protobuf.Any` type,
 *  should be listed here by name. Example:
 *  types:
 *  - name: google.protobuf.Int32
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Type *> *types;

/** Configuration controlling usage of this service. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Usage *usage;

@end


/**
 *  The per-product per-project service identity for a service.
 *  Use this field to configure per-product per-project service identity.
 *  Example of a service identity configuration.
 *  usage:
 *  service_identity:
 *  - service_account_parent: "projects/123456789"
 *  display_name: "Cloud XXX Service Agent"
 *  description: "Used as the identity of Cloud XXX to access resources"
 */
@interface GTLRServiceManagement_ServiceIdentity : GTLRObject

/**
 *  Optional. A user-specified opaque description of the service account.
 *  Must be less than or equal to 256 UTF-8 bytes.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  Optional. A user-specified name for the service account.
 *  Must be less than or equal to 100 UTF-8 bytes.
 */
@property(nonatomic, copy, nullable) NSString *displayName;

/**
 *  A service account project that hosts the service accounts.
 *  An example name would be:
 *  `projects/123456789`
 */
@property(nonatomic, copy, nullable) NSString *serviceAccountParent;

@end


/**
 *  Request message for `SetIamPolicy` method.
 */
@interface GTLRServiceManagement_SetIamPolicyRequest : GTLRObject

/**
 *  REQUIRED: The complete policy to be applied to the `resource`. The size of
 *  the policy is limited to a few 10s of KB. An empty policy is a
 *  valid policy but certain Cloud Platform services (such as Projects)
 *  might reject them.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Policy *policy;

/**
 *  OPTIONAL: A FieldMask specifying which fields of the policy to modify. Only
 *  the fields in the mask will be modified. If no mask is provided, the
 *  following default mask is used:
 *  paths: "bindings, etag"
 *  This field is only used by Cloud IAM.
 *
 *  String format is a comma-separated list of fields.
 */
@property(nonatomic, copy, nullable) NSString *updateMask;

@end


/**
 *  `SourceContext` represents information about the source of a
 *  protobuf element, like the file in which it is defined.
 */
@interface GTLRServiceManagement_SourceContext : GTLRObject

/**
 *  The path-qualified name of the .proto file that contained the associated
 *  protobuf element. For example: `"google/protobuf/source_context.proto"`.
 */
@property(nonatomic, copy, nullable) NSString *fileName;

@end


/**
 *  Source information used to create a Service Config
 */
@interface GTLRServiceManagement_SourceInfo : GTLRObject

/** All files used during config generation. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_SourceInfo_SourceFiles_Item *> *sourceFiles;

@end


/**
 *  GTLRServiceManagement_SourceInfo_SourceFiles_Item
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_SourceInfo_SourceFiles_Item : GTLRObject
@end


/**
 *  The `Status` type defines a logical error model that is suitable for
 *  different programming environments, including REST APIs and RPC APIs. It is
 *  used by [gRPC](https://github.com/grpc). Each `Status` message contains
 *  three pieces of data: error code, error message, and error details.
 *  You can find out more about this error model and how to work with it in the
 *  [API Design Guide](https://cloud.google.com/apis/design/errors).
 */
@interface GTLRServiceManagement_Status : GTLRObject

/**
 *  The status code, which should be an enum value of google.rpc.Code.
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *code;

/**
 *  A list of messages that carry the error details. There is a common set of
 *  message types for APIs to use.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Status_Details_Item *> *details;

/**
 *  A developer-facing error message, which should be in English. Any
 *  user-facing error message should be localized and sent in the
 *  google.rpc.Status.details field, or localized by the client.
 */
@property(nonatomic, copy, nullable) NSString *message;

@end


/**
 *  GTLRServiceManagement_Status_Details_Item
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_Status_Details_Item : GTLRObject
@end


/**
 *  Represents the status of one operation step.
 */
@interface GTLRServiceManagement_Step : GTLRObject

/**
 *  The short description of the step.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, copy, nullable) NSString *descriptionProperty;

/**
 *  The status code.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Step_Status_Cancelled The operation or step
 *        has completed with cancellation. (Value: "CANCELLED")
 *    @arg @c kGTLRServiceManagement_Step_Status_Done The operation or step has
 *        completed without errors. (Value: "DONE")
 *    @arg @c kGTLRServiceManagement_Step_Status_Failed The operation or step
 *        has completed with errors. If the operation is
 *        rollbackable, the rollback completed with errors too. (Value:
 *        "FAILED")
 *    @arg @c kGTLRServiceManagement_Step_Status_InProgress The operation or
 *        step is in progress. (Value: "IN_PROGRESS")
 *    @arg @c kGTLRServiceManagement_Step_Status_NotStarted The operation or
 *        step has not started yet. (Value: "NOT_STARTED")
 *    @arg @c kGTLRServiceManagement_Step_Status_StatusUnspecified Unspecifed
 *        code. (Value: "STATUS_UNSPECIFIED")
 */
@property(nonatomic, copy, nullable) NSString *status;

@end


/**
 *  Request message for SubmitConfigSource method.
 */
@interface GTLRServiceManagement_SubmitConfigSourceRequest : GTLRObject

/** Required. The source configuration for the service. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_ConfigSource *configSource;

/**
 *  Optional. If set, this will result in the generation of a
 *  `google.api.Service` configuration based on the `ConfigSource` provided,
 *  but the generated config and the sources will NOT be persisted.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *validateOnly;

@end


/**
 *  Response message for SubmitConfigSource method.
 */
@interface GTLRServiceManagement_SubmitConfigSourceResponse : GTLRObject

/** The generated service configuration. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_Service *serviceConfig;

@end


/**
 *  Define a parameter's name and location. The parameter may be passed as
 *  either
 *  an HTTP header or a URL query parameter, and if both are passed the behavior
 *  is implementation-dependent.
 */
@interface GTLRServiceManagement_SystemParameter : GTLRObject

/**
 *  Define the HTTP header name to use for the parameter. It is case
 *  insensitive.
 */
@property(nonatomic, copy, nullable) NSString *httpHeader;

/**
 *  Define the name of the parameter, such as "api_key" . It is case sensitive.
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  Define the URL query parameter name to use for the parameter. It is case
 *  sensitive.
 */
@property(nonatomic, copy, nullable) NSString *urlQueryParameter;

@end


/**
 *  Define a system parameter rule mapping system parameter definitions to
 *  methods.
 */
@interface GTLRServiceManagement_SystemParameterRule : GTLRObject

/**
 *  Define parameters. Multiple names may be defined for a parameter.
 *  For a given method call, only one of them should be used. If multiple
 *  names are used the behavior is implementation-dependent.
 *  If none of the specified names are present the behavior is
 *  parameter-dependent.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_SystemParameter *> *parameters;

/**
 *  Selects the methods to which this rule applies. Use '*' to indicate all
 *  methods in all APIs.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

@end


/**
 *  ### System parameter configuration
 *  A system parameter is a special kind of parameter defined by the API
 *  system, not by an individual API. It is typically mapped to an HTTP header
 *  and/or a URL query parameter. This configuration specifies which methods
 *  change the names of the system parameters.
 */
@interface GTLRServiceManagement_SystemParameters : GTLRObject

/**
 *  Define system parameters.
 *  The parameters defined here will override the default parameters
 *  implemented by the system. If this field is missing from the service
 *  config, default system parameters will be used. Default system parameters
 *  and names is implementation-dependent.
 *  Example: define api key for all methods
 *  system_parameters
 *  rules:
 *  - selector: "*"
 *  parameters:
 *  - name: api_key
 *  url_query_parameter: api_key
 *  Example: define 2 api key names for a specific method.
 *  system_parameters
 *  rules:
 *  - selector: "/ListShelves"
 *  parameters:
 *  - name: api_key
 *  http_header: Api-Key1
 *  - name: api_key
 *  http_header: Api-Key2
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_SystemParameterRule *> *rules;

@end


/**
 *  Request message for `TestIamPermissions` method.
 */
@interface GTLRServiceManagement_TestIamPermissionsRequest : GTLRObject

/**
 *  The set of permissions to check for the `resource`. Permissions with
 *  wildcards (such as '*' or 'storage.*') are not allowed. For more
 *  information see
 *  [IAM Overview](https://cloud.google.com/iam/docs/overview#permissions).
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *permissions;

@end


/**
 *  Response message for `TestIamPermissions` method.
 */
@interface GTLRServiceManagement_TestIamPermissionsResponse : GTLRObject

/**
 *  A subset of `TestPermissionsRequest.permissions` that the caller is
 *  allowed.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *permissions;

@end


/**
 *  Strategy that specifies how clients of Google Service Controller want to
 *  send traffic to use different config versions. This is generally
 *  used by API proxy to split traffic based on your configured percentage for
 *  each config version.
 *  One example of how to gradually rollout a new service configuration using
 *  this
 *  strategy:
 *  Day 1
 *  Rollout {
 *  id: "example.googleapis.com/rollout_20160206"
 *  traffic_percent_strategy {
 *  percentages: {
 *  "example.googleapis.com/20160201": 70.00
 *  "example.googleapis.com/20160206": 30.00
 *  }
 *  }
 *  }
 *  Day 2
 *  Rollout {
 *  id: "example.googleapis.com/rollout_20160207"
 *  traffic_percent_strategy: {
 *  percentages: {
 *  "example.googleapis.com/20160206": 100.00
 *  }
 *  }
 *  }
 */
@interface GTLRServiceManagement_TrafficPercentStrategy : GTLRObject

/**
 *  Maps service configuration IDs to their corresponding traffic percentage.
 *  Key is the service configuration ID, Value is the traffic percentage
 *  which must be greater than 0.0 and the sum must equal to 100.0.
 */
@property(nonatomic, strong, nullable) GTLRServiceManagement_TrafficPercentStrategy_Percentages *percentages;

@end


/**
 *  Maps service configuration IDs to their corresponding traffic percentage.
 *  Key is the service configuration ID, Value is the traffic percentage
 *  which must be greater than 0.0 and the sum must equal to 100.0.
 *
 *  @note This class is documented as having more properties of NSNumber (Uses
 *        NSNumber of doubleValue.). Use @c -additionalJSONKeys and @c
 *        -additionalPropertyForName: to get the list of properties and then
 *        fetch them; or @c -additionalProperties to fetch them all at once.
 */
@interface GTLRServiceManagement_TrafficPercentStrategy_Percentages : GTLRObject
@end


/**
 *  A protocol buffer message type.
 */
@interface GTLRServiceManagement_Type : GTLRObject

/** The list of fields. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Field *> *fields;

/** The fully qualified message name. */
@property(nonatomic, copy, nullable) NSString *name;

/** The list of types appearing in `oneof` definitions in this type. */
@property(nonatomic, strong, nullable) NSArray<NSString *> *oneofs;

/** The protocol buffer options. */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_Option *> *options;

/** The source context. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_SourceContext *sourceContext;

/**
 *  The source syntax.
 *
 *  Likely values:
 *    @arg @c kGTLRServiceManagement_Type_Syntax_SyntaxProto2 Syntax `proto2`.
 *        (Value: "SYNTAX_PROTO2")
 *    @arg @c kGTLRServiceManagement_Type_Syntax_SyntaxProto3 Syntax `proto3`.
 *        (Value: "SYNTAX_PROTO3")
 */
@property(nonatomic, copy, nullable) NSString *syntax;

@end


/**
 *  Response message for UndeleteService method.
 */
@interface GTLRServiceManagement_UndeleteServiceResponse : GTLRObject

/** Revived service resource. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_ManagedService *service;

@end


/**
 *  Configuration controlling usage of a service.
 */
@interface GTLRServiceManagement_Usage : GTLRObject

/**
 *  The full resource name of a channel used for sending notifications to the
 *  service producer.
 *  Google Service Management currently only supports
 *  [Google Cloud Pub/Sub](https://cloud.google.com/pubsub) as a notification
 *  channel. To use Google Cloud Pub/Sub as the channel, this must be the name
 *  of a Cloud Pub/Sub topic that uses the Cloud Pub/Sub topic name format
 *  documented in https://cloud.google.com/pubsub/docs/overview.
 */
@property(nonatomic, copy, nullable) NSString *producerNotificationChannel;

/**
 *  Requirements that must be satisfied before a consumer project can use the
 *  service. Each requirement is of the form <service.name>/<requirement-id>;
 *  for example 'serviceusage.googleapis.com/billing-enabled'.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *requirements;

/**
 *  A list of usage rules that apply to individual API methods.
 *  **NOTE:** All service configuration rules follow "last one wins" order.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRServiceManagement_UsageRule *> *rules;

/** The configuration of a per-product per-project service identity. */
@property(nonatomic, strong, nullable) GTLRServiceManagement_ServiceIdentity *serviceIdentity;

@end


/**
 *  Usage configuration rules for the service.
 *  NOTE: Under development.
 *  Use this rule to configure unregistered calls for the service. Unregistered
 *  calls are calls that do not contain consumer project identity.
 *  (Example: calls that do not contain an API key).
 *  By default, API methods do not allow unregistered calls, and each method
 *  call
 *  must be identified by a consumer project identity. Use this rule to
 *  allow/disallow unregistered calls.
 *  Example of an API that wants to allow unregistered calls for entire service.
 *  usage:
 *  rules:
 *  - selector: "*"
 *  allow_unregistered_calls: true
 *  Example of a method that wants to allow unregistered calls.
 *  usage:
 *  rules:
 *  - selector: "google.example.library.v1.LibraryService.CreateBook"
 *  allow_unregistered_calls: true
 */
@interface GTLRServiceManagement_UsageRule : GTLRObject

/**
 *  If true, the selected method allows unregistered calls, e.g. calls
 *  that don't identify any user or application.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *allowUnregisteredCalls;

/**
 *  Selects the methods to which this rule applies. Use '*' to indicate all
 *  methods in all APIs.
 *  Refer to selector for syntax details.
 */
@property(nonatomic, copy, nullable) NSString *selector;

/**
 *  If true, the selected method should skip service control and the control
 *  plane features, such as quota and billing, will not be available.
 *  This flag is used by Google Cloud Endpoints to bypass checks for internal
 *  methods, such as service health check methods.
 *
 *  Uses NSNumber of boolValue.
 */
@property(nonatomic, strong, nullable) NSNumber *skipServiceControl;

@end

NS_ASSUME_NONNULL_END

#pragma clang diagnostic pop
