// NOTE: This file was generated by the ServiceGenerator.

// ----------------------------------------------------------------------------
// API:
//   Google Maps Coordinate API (coordinate/v1)
// Description:
//   Lets you view and manage jobs in a Coordinate team.
// Documentation:
//   https://developers.google.com/coordinate/

#if GTLR_BUILT_AS_FRAMEWORK
  #import "GTLR/GTLRObject.h"
#else
  #import "GTLRObject.h"
#endif

#if GTLR_RUNTIME_VERSION != 3000
#error This file was generated by a different version of ServiceGenerator which is incompatible with this GTLR library source.
#endif

@class GTLRCoordinate_CustomField;
@class GTLRCoordinate_CustomFieldDef;
@class GTLRCoordinate_CustomFields;
@class GTLRCoordinate_EnumItemDef;
@class GTLRCoordinate_Job;
@class GTLRCoordinate_JobChange;
@class GTLRCoordinate_JobState;
@class GTLRCoordinate_Location;
@class GTLRCoordinate_LocationRecord;
@class GTLRCoordinate_Team;
@class GTLRCoordinate_TokenPagination;
@class GTLRCoordinate_Worker;

NS_ASSUME_NONNULL_BEGIN

/**
 *  Custom field.
 */
@interface GTLRCoordinate_CustomField : GTLRObject

/**
 *  Custom field id.
 *
 *  Uses NSNumber of longLongValue.
 */
@property(strong, nullable) NSNumber *customFieldId;

/** Identifies this object as a custom field. */
@property(copy, nullable) NSString *kind;

/** Custom field value. */
@property(copy, nullable) NSString *value;

@end


/**
 *  Custom field definition.
 */
@interface GTLRCoordinate_CustomFieldDef : GTLRObject

/**
 *  Whether the field is enabled.
 *
 *  Uses NSNumber of boolValue.
 */
@property(strong, nullable) NSNumber *enabled;

/**
 *  List of enum items for this custom field. Populated only if the field type
 *  is enum. Enum fields appear as 'lists' in the Coordinate web and mobile UI.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_EnumItemDef *> *enumitems;

/**
 *  Custom field id.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 *
 *  Uses NSNumber of longLongValue.
 */
@property(strong, nullable) NSNumber *identifier;

/** Identifies this object as a custom field definition. */
@property(copy, nullable) NSString *kind;

/** Custom field name. */
@property(copy, nullable) NSString *name;

/**
 *  Whether the field is required for checkout.
 *
 *  Uses NSNumber of boolValue.
 */
@property(strong, nullable) NSNumber *requiredForCheckout;

/** Custom field type. */
@property(copy, nullable) NSString *type;

@end


/**
 *  Collection of custom field definitions for a team.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "items" property.
 */
@interface GTLRCoordinate_CustomFieldDefListResponse : GTLRCollectionObject

/**
 *  Collection of custom field definitions in a team.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_CustomFieldDef *> *items;

/**
 *  Identifies this object as a collection of custom field definitions in a
 *  team.
 */
@property(copy, nullable) NSString *kind;

@end


/**
 *  Collection of custom fields.
 */
@interface GTLRCoordinate_CustomFields : GTLRObject

/** Collection of custom fields. */
@property(strong, nullable) NSArray<GTLRCoordinate_CustomField *> *customField;

/** Identifies this object as a collection of custom fields. */
@property(copy, nullable) NSString *kind;

@end


/**
 *  Enum Item definition.
 */
@interface GTLRCoordinate_EnumItemDef : GTLRObject

/**
 *  Whether the enum item is active. Jobs may contain inactive enum values;
 *  however, setting an enum to an inactive value when creating or updating a
 *  job will result in a 500 error.
 *
 *  Uses NSNumber of boolValue.
 */
@property(strong, nullable) NSNumber *active;

/** Identifies this object as an enum item definition. */
@property(copy, nullable) NSString *kind;

/** Custom field value. */
@property(copy, nullable) NSString *value;

@end


/**
 *  A job.
 */
@interface GTLRCoordinate_Job : GTLRObject

/**
 *  Job id.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 *
 *  Uses NSNumber of unsignedLongLongValue.
 */
@property(strong, nullable) NSNumber *identifier;

/**
 *  List of job changes since it was created. The first change corresponds to
 *  the state of the job when it was created.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_JobChange *> *jobChange;

/** Identifies this object as a job. */
@property(copy, nullable) NSString *kind;

/** Current job state. */
@property(strong, nullable) GTLRCoordinate_JobState *state;

@end


/**
 *  Change to a job. For example assigning the job to a different worker.
 */
@interface GTLRCoordinate_JobChange : GTLRObject

/** Identifies this object as a job change. */
@property(copy, nullable) NSString *kind;

/** Change applied to the job. Only the fields that were changed are set. */
@property(strong, nullable) GTLRCoordinate_JobState *state;

/**
 *  Time at which this change was applied.
 *
 *  Uses NSNumber of unsignedLongLongValue.
 */
@property(strong, nullable) NSNumber *timestamp;

@end


/**
 *  Response from a List Jobs request.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "items" property. If returned as the result of a query, it should
 *        support automatic pagination (when @c shouldFetchNextPages is
 *        enabled).
 */
@interface GTLRCoordinate_JobListResponse : GTLRCollectionObject

/**
 *  Jobs in the collection.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_Job *> *items;

/** Identifies this object as a list of jobs. */
@property(copy, nullable) NSString *kind;

/** A token to provide to get the next page of results. */
@property(copy, nullable) NSString *nextPageToken;

@end


/**
 *  Current state of a job.
 */
@interface GTLRCoordinate_JobState : GTLRObject

/**
 *  Email address of the assignee, or the string "DELETED_USER" if the account
 *  is no longer available.
 */
@property(copy, nullable) NSString *assignee;

/** Customer name. */
@property(copy, nullable) NSString *customerName;

/** Customer phone number. */
@property(copy, nullable) NSString *customerPhoneNumber;

/** Custom fields. */
@property(strong, nullable) GTLRCoordinate_CustomFields *customFields;

/** Identifies this object as a job state. */
@property(copy, nullable) NSString *kind;

/** Job location. */
@property(strong, nullable) GTLRCoordinate_Location *location;

/** Note added to the job. */
@property(strong, nullable) NSArray<NSString *> *note;

/** Job progress. */
@property(copy, nullable) NSString *progress;

/** Job title. */
@property(copy, nullable) NSString *title;

@end


/**
 *  Location of a job.
 */
@interface GTLRCoordinate_Location : GTLRObject

/** Address. */
@property(strong, nullable) NSArray<NSString *> *addressLine;

/** Identifies this object as a location. */
@property(copy, nullable) NSString *kind;

/**
 *  Latitude.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(strong, nullable) NSNumber *lat;

/**
 *  Longitude.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(strong, nullable) NSNumber *lng;

@end


/**
 *  Response from a List Locations request.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "items" property. If returned as the result of a query, it should
 *        support automatic pagination (when @c shouldFetchNextPages is
 *        enabled).
 */
@interface GTLRCoordinate_LocationListResponse : GTLRCollectionObject

/**
 *  Locations in the collection.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_LocationRecord *> *items;

/** Identifies this object as a list of locations. */
@property(copy, nullable) NSString *kind;

/** A token to provide to get the next page of results. */
@property(copy, nullable) NSString *nextPageToken;

/** Pagination information for token pagination. */
@property(strong, nullable) GTLRCoordinate_TokenPagination *tokenPagination;

@end


/**
 *  Recorded location of a worker.
 */
@interface GTLRCoordinate_LocationRecord : GTLRObject

/**
 *  The collection time in milliseconds since the epoch.
 *
 *  Uses NSNumber of longLongValue.
 */
@property(strong, nullable) NSNumber *collectionTime;

/**
 *  The location accuracy in meters. This is the radius of a 95% confidence
 *  interval around the location measurement.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(strong, nullable) NSNumber *confidenceRadius;

/** Identifies this object as a location. */
@property(copy, nullable) NSString *kind;

/**
 *  Latitude.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(strong, nullable) NSNumber *latitude;

/**
 *  Longitude.
 *
 *  Uses NSNumber of doubleValue.
 */
@property(strong, nullable) NSNumber *longitude;

@end


/**
 *  Job schedule.
 */
@interface GTLRCoordinate_Schedule : GTLRObject

/**
 *  Whether the job is scheduled for the whole day. Time of day in start/end
 *  times is ignored if this is true.
 *
 *  Uses NSNumber of boolValue.
 */
@property(strong, nullable) NSNumber *allDay;

/**
 *  Job duration in milliseconds.
 *
 *  Uses NSNumber of unsignedLongLongValue.
 */
@property(strong, nullable) NSNumber *duration;

/**
 *  Scheduled end time in milliseconds since epoch.
 *
 *  Uses NSNumber of unsignedLongLongValue.
 */
@property(strong, nullable) NSNumber *endTime;

/** Identifies this object as a job schedule. */
@property(copy, nullable) NSString *kind;

/**
 *  Scheduled start time in milliseconds since epoch.
 *
 *  Uses NSNumber of unsignedLongLongValue.
 */
@property(strong, nullable) NSNumber *startTime;

@end


/**
 *  A Coordinate team.
 */
@interface GTLRCoordinate_Team : GTLRObject

/**
 *  Team id, as found in a coordinate team url e.g.
 *  https://coordinate.google.com/f/xyz where "xyz" is the team id.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 */
@property(copy, nullable) NSString *identifier;

/** Identifies this object as a team. */
@property(copy, nullable) NSString *kind;

/** Team name */
@property(copy, nullable) NSString *name;

@end


/**
 *  Response from a List Teams request.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "items" property.
 */
@interface GTLRCoordinate_TeamListResponse : GTLRCollectionObject

/**
 *  Teams in the collection.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_Team *> *items;

/** Identifies this object as a list of teams. */
@property(copy, nullable) NSString *kind;

@end


/**
 *  Pagination information.
 */
@interface GTLRCoordinate_TokenPagination : GTLRObject

/** Identifies this object as pagination information. */
@property(copy, nullable) NSString *kind;

/** A token to provide to get the next page of results. */
@property(copy, nullable) NSString *nextPageToken;

/** A token to provide to get the previous page of results. */
@property(copy, nullable) NSString *previousPageToken;

@end


/**
 *  A worker in a Coordinate team.
 */
@interface GTLRCoordinate_Worker : GTLRObject

/**
 *  Worker email address. If a worker has been deleted from your team, the email
 *  address will appear as DELETED_USER.
 *
 *  identifier property maps to 'id' in JSON (to avoid Objective C's 'id').
 */
@property(copy, nullable) NSString *identifier;

/** Identifies this object as a worker. */
@property(copy, nullable) NSString *kind;

@end


/**
 *  Response from a List Workers request.
 *
 *  @note This class supports NSFastEnumeration and indexed subscripting over
 *        its "items" property.
 */
@interface GTLRCoordinate_WorkerListResponse : GTLRCollectionObject

/**
 *  Workers in the collection.
 *
 *  @note This property is used to support NSFastEnumeration and indexed
 *        subscripting on this class.
 */
@property(strong, nullable) NSArray<GTLRCoordinate_Worker *> *items;

/** Identifies this object as a list of workers. */
@property(copy, nullable) NSString *kind;

@end

NS_ASSUME_NONNULL_END
