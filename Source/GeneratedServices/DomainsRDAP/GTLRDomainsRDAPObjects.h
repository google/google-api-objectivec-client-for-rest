// NOTE: This file was generated by the ServiceGenerator.

// ----------------------------------------------------------------------------
// API:
//   Domains RDAP API (domainsrdap/v1)
// Description:
//   Read-only public API that lets users search for information about domain
//   names.
// Documentation:
//   https://developers.google.com/domains/rdap/

#if SWIFT_PACKAGE || GTLR_USE_MODULAR_IMPORT
  @import GoogleAPIClientForRESTCore;
#elif GTLR_BUILT_AS_FRAMEWORK
  #import "GTLR/GTLRObject.h"
#else
  #import "GTLRObject.h"
#endif

#if GTLR_RUNTIME_VERSION != 3000
#error This file was generated by a different version of ServiceGenerator which is incompatible with this GTLR library source.
#endif

@class GTLRDomainsRDAP_HttpBody;
@class GTLRDomainsRDAP_HttpBody_Extensions_Item;
@class GTLRDomainsRDAP_Link;
@class GTLRDomainsRDAP_Notice;

// Generated comments include content from the discovery document; avoid them
// causing warnings since clang's checks are some what arbitrary.
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdocumentation"

NS_ASSUME_NONNULL_BEGIN

/**
 *  Message that represents an arbitrary HTTP body. It should only be used for
 *  payload formats that can't be represented as JSON, such as raw binary or
 *  an HTML page.
 *  This message can be used both in streaming and non-streaming API methods in
 *  the request as well as the response.
 *  It can be used as a top-level request field, which is convenient if one
 *  wants to extract parameters from either the URL or HTTP template into the
 *  request fields and also want access to the raw HTTP body.
 *  Example:
 *  message GetResourceRequest {
 *  // A unique request id.
 *  string request_id = 1;
 *  // The raw HTTP body is bound to this field.
 *  google.api.HttpBody http_body = 2;
 *  }
 *  service ResourceService {
 *  rpc GetResource(GetResourceRequest) returns (google.api.HttpBody);
 *  rpc UpdateResource(google.api.HttpBody) returns
 *  (google.protobuf.Empty);
 *  }
 *  Example with streaming methods:
 *  service CaldavService {
 *  rpc GetCalendar(stream google.api.HttpBody)
 *  returns (stream google.api.HttpBody);
 *  rpc UpdateCalendar(stream google.api.HttpBody)
 *  returns (stream google.api.HttpBody);
 *  }
 *  Use of this type only changes how the request and response bodies are
 *  handled, all other features will continue to work unchanged.
 */
@interface GTLRDomainsRDAP_HttpBody : GTLRObject

/**
 *  The HTTP Content-Type header value specifying the content type of the body.
 */
@property(nonatomic, copy, nullable) NSString *contentType;

/**
 *  The HTTP request/response body as raw binary.
 *
 *  Contains encoded binary data; GTLRBase64 can encode/decode (probably
 *  web-safe format).
 */
@property(nonatomic, copy, nullable) NSString *data;

/**
 *  Application specific response metadata. Must be set in the first response
 *  for streaming APIs.
 */
@property(nonatomic, strong, nullable) NSArray<GTLRDomainsRDAP_HttpBody_Extensions_Item *> *extensions;

@end


/**
 *  GTLRDomainsRDAP_HttpBody_Extensions_Item
 *
 *  @note This class is documented as having more properties of any valid JSON
 *        type. Use @c -additionalJSONKeys and @c -additionalPropertyForName: to
 *        get the list of properties and then fetch them; or @c
 *        -additionalProperties to fetch them all at once.
 */
@interface GTLRDomainsRDAP_HttpBody_Extensions_Item : GTLRObject
@end


/**
 *  Links object defined in [section 4.2 of RFC
 *  7483](https://tools.ietf.org/html/rfc7483#section-4.2).
 */
@interface GTLRDomainsRDAP_Link : GTLRObject

/** Target URL of a link. Example: "http://example.com/previous". */
@property(nonatomic, copy, nullable) NSString *href;

/** Language code of a link. Example: "en". */
@property(nonatomic, copy, nullable) NSString *hreflang;

/** Media type of the link destination. Example: "screen". */
@property(nonatomic, copy, nullable) NSString *media;

/** Relation type of a link. Example: "previous". */
@property(nonatomic, copy, nullable) NSString *rel;

/** Title of this link. Example: "title". */
@property(nonatomic, copy, nullable) NSString *title;

/** Content type of the link. Example: "application/json". */
@property(nonatomic, copy, nullable) NSString *type;

/** URL giving context for the link. Example: "http://example.com/current". */
@property(nonatomic, copy, nullable) NSString *value;

@end


/**
 *  Notices object defined in [section 4.3 of RFC
 *  7483](https://tools.ietf.org/html/rfc7483#section-4.3).
 */
@interface GTLRDomainsRDAP_Notice : GTLRObject

/**
 *  Description of the notice.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *descriptionProperty;

/** Link to a document containing more information. */
@property(nonatomic, strong, nullable) NSArray<GTLRDomainsRDAP_Link *> *links;

/** Title of a notice. Example: "Terms of Service". */
@property(nonatomic, copy, nullable) NSString *title;

/**
 *  Type values defined in [section 10.2.1 of RFC
 *  7483](https://tools.ietf.org/html/rfc7483#section-10.2.1) specific to a
 *  whole response: "result set truncated due to authorization", "result set
 *  truncated due to excessive load", "result set truncated due to
 *  unexplainable reasons".
 */
@property(nonatomic, copy, nullable) NSString *type;

@end


/**
 *  Response to a general RDAP query.
 */
@interface GTLRDomainsRDAP_RdapResponse : GTLRObject

/**
 *  Error description.
 *
 *  Remapped to 'descriptionProperty' to avoid NSObject's 'description'.
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *descriptionProperty;

/**
 *  Error HTTP code. Example: "501".
 *
 *  Uses NSNumber of intValue.
 */
@property(nonatomic, strong, nullable) NSNumber *errorCode;

/** HTTP response with content type set to "application/json+rdap". */
@property(nonatomic, strong, nullable) GTLRDomainsRDAP_HttpBody *jsonResponse;

/**
 *  Error language code. Error response info fields are defined in [section 6
 *  of RFC 7483](https://tools.ietf.org/html/rfc7483#section-6).
 */
@property(nonatomic, copy, nullable) NSString *lang;

/** Notices applying to this response. */
@property(nonatomic, strong, nullable) NSArray<GTLRDomainsRDAP_Notice *> *notices;

/** RDAP conformance level. */
@property(nonatomic, strong, nullable) NSArray<NSString *> *rdapConformance;

/** Error title. */
@property(nonatomic, copy, nullable) NSString *title;

@end

NS_ASSUME_NONNULL_END

#pragma clang diagnostic pop
