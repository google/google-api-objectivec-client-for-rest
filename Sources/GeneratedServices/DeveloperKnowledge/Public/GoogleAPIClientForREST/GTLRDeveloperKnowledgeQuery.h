// NOTE: This file was generated by the ServiceGenerator.

// ----------------------------------------------------------------------------
// API:
//   Developer Knowledge API (developerknowledge/v1)
// Description:
//   The Developer Knowledge API provides access to Google's developer
//   knowledge.
// Documentation:
//   https://developers.google.com/knowledge

#import <GoogleAPIClientForREST/GTLRQuery.h>

#if GTLR_RUNTIME_VERSION != 3000
#error This file was generated by a different version of ServiceGenerator which is incompatible with this GTLR library source.
#endif

// Generated comments include content from the discovery document; avoid them
// causing warnings since clang's checks are some what arbitrary.
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wdocumentation"

NS_ASSUME_NONNULL_BEGIN

// ----------------------------------------------------------------------------
// Constants - For some of the query classes' properties below.

// ----------------------------------------------------------------------------
// view

/**
 *  Includes only the basic metadata fields: - `name` - `uri` - `data_source` -
 *  `title` - `description` - `update_time` - `view` This is the default of view
 *  for DeveloperKnowledge.SearchDocumentChunks.
 *
 *  Value: "DOCUMENT_VIEW_BASIC"
 */
FOUNDATION_EXTERN NSString * const kGTLRDeveloperKnowledgeViewDocumentViewBasic;
/**
 *  Includes the `DOCUMENT_VIEW_BASIC` fields and the `content` field. This is
 *  the default of view for DeveloperKnowledge.GetDocument and
 *  DeveloperKnowledge.BatchGetDocuments.
 *
 *  Value: "DOCUMENT_VIEW_CONTENT"
 */
FOUNDATION_EXTERN NSString * const kGTLRDeveloperKnowledgeViewDocumentViewContent;
/**
 *  Includes all Document fields.
 *
 *  Value: "DOCUMENT_VIEW_FULL"
 */
FOUNDATION_EXTERN NSString * const kGTLRDeveloperKnowledgeViewDocumentViewFull;
/**
 *  The default / unset value. See each API method for its default value if
 *  DocumentView is not specified.
 *
 *  Value: "DOCUMENT_VIEW_UNSPECIFIED"
 */
FOUNDATION_EXTERN NSString * const kGTLRDeveloperKnowledgeViewDocumentViewUnspecified;

// ----------------------------------------------------------------------------
// Query Classes
//

/**
 *  Parent class for other Developer Knowledge query classes.
 */
@interface GTLRDeveloperKnowledgeQuery : GTLRQuery

/** Selector specifying which fields to include in a partial response. */
@property(nonatomic, copy, nullable) NSString *fields;

@end

/**
 *  Retrieves multiple documents, each with its full Markdown content.
 *
 *  Method: developerknowledge.documents.batchGet
 *
 *  Authorization scope(s):
 *    @c kGTLRAuthScopeDeveloperKnowledgeCloudPlatform
 */
@interface GTLRDeveloperKnowledgeQuery_DocumentsBatchGet : GTLRDeveloperKnowledgeQuery

/**
 *  Required. Specifies the names of the documents to retrieve. A maximum of 20
 *  documents can be retrieved in a batch. The documents are returned in the
 *  same order as the `names` in the request. Format:
 *  `documents/{uri_without_scheme}` Example:
 *  `documents/docs.cloud.google.com/storage/docs/creating-buckets`
 */
@property(nonatomic, strong, nullable) NSArray<NSString *> *names;

/**
 *  Optional. Specifies the DocumentView of the document. If unspecified,
 *  DeveloperKnowledge.BatchGetDocuments defaults to `DOCUMENT_VIEW_CONTENT`.
 *
 *  Likely values:
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewUnspecified The default /
 *        unset value. See each API method for its default value if DocumentView
 *        is not specified. (Value: "DOCUMENT_VIEW_UNSPECIFIED")
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewBasic Includes only the
 *        basic metadata fields: - `name` - `uri` - `data_source` - `title` -
 *        `description` - `update_time` - `view` This is the default of view for
 *        DeveloperKnowledge.SearchDocumentChunks. (Value:
 *        "DOCUMENT_VIEW_BASIC")
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewFull Includes all Document
 *        fields. (Value: "DOCUMENT_VIEW_FULL")
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewContent Includes the
 *        `DOCUMENT_VIEW_BASIC` fields and the `content` field. This is the
 *        default of view for DeveloperKnowledge.GetDocument and
 *        DeveloperKnowledge.BatchGetDocuments. (Value: "DOCUMENT_VIEW_CONTENT")
 */
@property(nonatomic, copy, nullable) NSString *view;

/**
 *  Fetches a @c GTLRDeveloperKnowledge_BatchGetDocumentsResponse.
 *
 *  Retrieves multiple documents, each with its full Markdown content.
 *
 *  @return GTLRDeveloperKnowledgeQuery_DocumentsBatchGet
 */
+ (instancetype)query;

@end

/**
 *  Retrieves a single document with its full Markdown content.
 *
 *  Method: developerknowledge.documents.get
 *
 *  Authorization scope(s):
 *    @c kGTLRAuthScopeDeveloperKnowledgeCloudPlatform
 */
@interface GTLRDeveloperKnowledgeQuery_DocumentsGet : GTLRDeveloperKnowledgeQuery

/**
 *  Required. Specifies the name of the document to retrieve. Format:
 *  `documents/{uri_without_scheme}` Example:
 *  `documents/docs.cloud.google.com/storage/docs/creating-buckets`
 */
@property(nonatomic, copy, nullable) NSString *name;

/**
 *  Optional. Specifies the DocumentView of the document. If unspecified,
 *  DeveloperKnowledge.GetDocument defaults to `DOCUMENT_VIEW_CONTENT`.
 *
 *  Likely values:
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewUnspecified The default /
 *        unset value. See each API method for its default value if DocumentView
 *        is not specified. (Value: "DOCUMENT_VIEW_UNSPECIFIED")
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewBasic Includes only the
 *        basic metadata fields: - `name` - `uri` - `data_source` - `title` -
 *        `description` - `update_time` - `view` This is the default of view for
 *        DeveloperKnowledge.SearchDocumentChunks. (Value:
 *        "DOCUMENT_VIEW_BASIC")
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewFull Includes all Document
 *        fields. (Value: "DOCUMENT_VIEW_FULL")
 *    @arg @c kGTLRDeveloperKnowledgeViewDocumentViewContent Includes the
 *        `DOCUMENT_VIEW_BASIC` fields and the `content` field. This is the
 *        default of view for DeveloperKnowledge.GetDocument and
 *        DeveloperKnowledge.BatchGetDocuments. (Value: "DOCUMENT_VIEW_CONTENT")
 */
@property(nonatomic, copy, nullable) NSString *view;

/**
 *  Fetches a @c GTLRDeveloperKnowledge_Document.
 *
 *  Retrieves a single document with its full Markdown content.
 *
 *  @param name Required. Specifies the name of the document to retrieve.
 *    Format: `documents/{uri_without_scheme}` Example:
 *    `documents/docs.cloud.google.com/storage/docs/creating-buckets`
 *
 *  @return GTLRDeveloperKnowledgeQuery_DocumentsGet
 */
+ (instancetype)queryWithName:(NSString *)name;

@end

/**
 *  Searches for developer knowledge across Google's developer documentation.
 *  Returns DocumentChunks based on the user's query. There may be many chunks
 *  from the same Document. To retrieve full documents, use
 *  DeveloperKnowledge.GetDocument or DeveloperKnowledge.BatchGetDocuments with
 *  the DocumentChunk.parent returned in the
 *  SearchDocumentChunksResponse.results.
 *
 *  Method: developerknowledge.documents.searchDocumentChunks
 *
 *  Authorization scope(s):
 *    @c kGTLRAuthScopeDeveloperKnowledgeCloudPlatform
 */
@interface GTLRDeveloperKnowledgeQuery_DocumentsSearchDocumentChunks : GTLRDeveloperKnowledgeQuery

/**
 *  Optional. Applies a strict filter to the search results. The expression
 *  supports a subset of the syntax described at https://google.aip.dev/160.
 *  While `SearchDocumentChunks` returns DocumentChunks, the filter is applied
 *  to `DocumentChunk.document` fields. Supported fields for filtering: *
 *  `data_source` (STRING): The source of the document, e.g.
 *  `docs.cloud.google.com`. See
 *  https://developers.google.com/knowledge/reference/corpus-reference for the
 *  complete list of data sources in the corpus. * `update_time` (TIMESTAMP):
 *  The timestamp of when the document was last meaningfully updated. A
 *  meaningful update is one that changes document's markdown content or
 *  metadata. * `uri` (STRING): The document URI, e.g.
 *  `https://docs.cloud.google.com/bigquery/docs/tables`. STRING fields support
 *  `=` (equals) and `!=` (not equals) operators for **exact match** on the
 *  whole string. Partial match, prefix match, and regexp match are not
 *  supported. TIMESTAMP fields support `=`, `<`, `<=`, `>`, and `>=` operators.
 *  Timestamps must be in RFC-3339 format, e.g., `"2025-01-01T00:00:00Z"`. You
 *  can combine expressions using `AND`, `OR`, and `NOT` (or `-`) logical
 *  operators. `OR` has higher precedence than `AND`. Use parentheses for
 *  explicit precedence grouping. Examples: * `data_source =
 *  "docs.cloud.google.com" OR data_source = "firebase.google.com"` *
 *  `data_source != "firebase.google.com"` * `update_time <
 *  "2024-01-01T00:00:00Z"` * `update_time >= "2025-01-22T00:00:00Z" AND
 *  (data_source = "developer.chrome.com" OR data_source = "web.dev")` * `uri =
 *  "https://docs.cloud.google.com/release-notes"` The `filter` string must not
 *  exceed 500 characters; values longer than 500 characters will result in an
 *  `INVALID_ARGUMENT` error.
 */
@property(nonatomic, copy, nullable) NSString *filter;

/**
 *  Optional. Specifies the maximum number of results to return. The service may
 *  return fewer than this value. If unspecified, at most 5 results will be
 *  returned. The maximum value is 20; values above 20 will result in an
 *  INVALID_ARGUMENT error.
 */
@property(nonatomic, assign) NSInteger pageSize;

/**
 *  Optional. Contains a page token, received from a previous
 *  `SearchDocumentChunks` call. Provide this to retrieve the subsequent page.
 */
@property(nonatomic, copy, nullable) NSString *pageToken;

/**
 *  Required. Provides the raw query string provided by the user, such as "How
 *  to create a Cloud Storage bucket?".
 */
@property(nonatomic, copy, nullable) NSString *query;

/**
 *  Fetches a @c GTLRDeveloperKnowledge_SearchDocumentChunksResponse.
 *
 *  Searches for developer knowledge across Google's developer documentation.
 *  Returns DocumentChunks based on the user's query. There may be many chunks
 *  from the same Document. To retrieve full documents, use
 *  DeveloperKnowledge.GetDocument or DeveloperKnowledge.BatchGetDocuments with
 *  the DocumentChunk.parent returned in the
 *  SearchDocumentChunksResponse.results.
 *
 *  @return GTLRDeveloperKnowledgeQuery_DocumentsSearchDocumentChunks
 *
 *  @note Automatic pagination will be done when @c shouldFetchNextPages is
 *        enabled. See @c shouldFetchNextPages on @c GTLRService for more
 *        information.
 */
+ (instancetype)query;

@end

NS_ASSUME_NONNULL_END

#pragma clang diagnostic pop
